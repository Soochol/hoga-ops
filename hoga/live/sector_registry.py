"""0J/0U 로 구독할 업종·지수 코드 목록 — `ka20003` 응답에서 뽑은 정적 시드.

**정적으로 두는 이유**는 WS REG 가 세션 기동 시점에 필요한데 `ka20003` 응답은
그보다 늦게(첫 폴링) 오기 때문이다. 동적으로 하면 기동 직후 창이 비고, 그 창을
메우려고 REG 를 미루면 재연결마다 같은 지연이 반복된다. 업종 코드는 KRX 개편이
아니면 바뀌지 않으므로 시드가 맞는 자료다(`trading_days_seed` 와 같은 성격).

목록이 벤더와 어긋나도 **조용히 실패한다** — REG ACK 는 코드 유효성을 검사하지 않아
쓰레기 코드도 `rc=0` 을 받는다(2026-07-16 실측). 그래서 관측 카운터로 "구독했는데
틱이 0 인 코드" 를 드러내는 것이 이 시드의 짝이다(`KiwoomSessionManager`).

## 화면이 안 쓰는 코드도 넣는다

`604`(코스피고배당50)·`138`(KOSDAQ 100) 처럼 지금 카드에 안 나오는 코드도 구독한다.
슬롯이 무료라(아래) 뺄 이유가 없고, `ka20003` 이 주는 행 전부를 덮어 두면 나중에
화면이 늘어도 배선이 따라올 필요가 없다.

## 슬롯

**업종·지수 구독은 200 종목 한도에 세지 않는다**(2026-08-07 실측: 업종 5개 REG 뒤
종목 200개 4배치 전부 `rc=0`). VI(`1h`)와 같은 별도 축이다. 이 사실이 목록을
넓게 잡는 근거이고, 만약 벤더가 정책을 바꾸면 여기서 먼저 터진다 —
`sub_rejected` 가 아니라 종목 구독이 밀리는 형태로 나타나므로 슬롯 회계를 함께 볼 것.
"""
from __future__ import annotations

#: 코스피 종합 + 업종(대형/중형/소형 규모별 포함) + 변동성지수·전략지수.
_KOSPI: tuple[str, ...] = (
    "001",  # 종합(KOSPI)
    "002", "003", "004",  # 대형주·중형주·소형주
    "005", "006", "007", "008", "009", "010", "011", "012", "013", "014", "015",
    "016", "017", "018", "019", "020", "021", "024", "025", "026", "027", "028",
    "029", "030",
    "603",  # 변동성지수(VKOSPI) — `/market` 변동성 카드의 소스
    "604", "605",  # 코스피고배당50·코스피배당성장50
)

#: 코스닥 종합 + 업종 + 지수 상품.
_KOSDAQ: tuple[str, ...] = (
    "101",  # 종합(KOSDAQ)
    "103", "106", "107", "108", "110", "111", "115", "116", "117", "118", "119",
    "120", "121", "122", "123", "124", "125", "126", "127", "128", "129",
    "138", "139", "140",  # KOSDAQ 100·MID 300·SMALL
    "141",
    "142", "143", "144", "145",  # 우량·벤처·중견·신성장
    "150",  # KOSDAQ 150 — 지수 카드
    "151",  # 코스닥글로벌지수
    "160", "165",  # F-KOSDAQ150·인버스
)

#: `ka20003` 업종 목록에 **없지만** 0J 로는 오는 코드(2026-08-07 실측에서 틱 확인).
#: 지수 카드가 쓰므로 별도로 싣는다 — 목록의 출처가 갈린다는 사실을 이름으로 남긴다.
_INDEX_PRODUCTS: tuple[str, ...] = (
    "201",  # KOSPI 200
)

#: 구독 전체. 순서는 REG 배치에만 영향한다.
SECTOR_CODES: tuple[str, ...] = _KOSPI + _KOSDAQ + _INDEX_PRODUCTS

#: 지수 카드/하단 바가 쓰는 대표지수 → 업종코드. **KRX100 은 없다** —
#: `ka20003` 공간에 없고 0J 코드도 미확인이라, 그 카드만 폴링으로 남는다.
#: 확인되면 여기 한 줄을 늘리면 화면이 따라온다.
INDEX_ID_TO_SECTOR_CODE: dict[str, str] = {
    "KOSPI": "001",
    "KOSDAQ": "101",
    "KOSPI200": "201",
    "KOSDAQ150": "150",
}
