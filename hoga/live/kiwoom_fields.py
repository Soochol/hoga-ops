"""키움 WS 실시간 FID 상수 — 실측(2026-07-16 실계좌)에서 옮긴 단일 진실원.

KIS(위치기반 ^구분)와 달리 키움 REAL 프레임은 이름표(FID) dict다:
  {"trnm":"REAL","data":[{"type":"0D","item":"005930","values":{"41":"+6500",...}}]}
FID 인덱스가 키움 쪽에서 바뀌면 여기 한 곳만 고친다. 파서(kiwoom_frames)는
이 상수로 WsTick payload를 만든다 — KIS ws_frames 계약 키를 **전부 보존**하되,
키움만 주는 필드는 additive 로 덧붙인다(포트 계약; kiwoom_frames 헤더 참조).

프로그램매매(0w)는 수급 FID(202~213) 의미 확정 후 추가(플랜 2026-07-20 §PR-F3).
"""
from __future__ import annotations

# 구독 코드 venue 접미 ↔ venue 태그의 정본은 `kiwoom_venue` 다 — 키움은 0D payload 에
# 거래소 필드가 없어(0B 엔 9081 있음) 호가 venue 를 **구독한 코드 접미**로 부여한다
# (실측 2026-07-16). 이 모듈이 자체 사본 `{"_NX": "NXT"}` 를 들고 있었고
# `kiwoom_multi_quote` 에 **방향이 반대인 동명 상수**가 따로 있어, `split_venue`
# ("005930_AL") 이 `("005930_AL", "KRX")` 를 돌려줬다(#1124). 기존 소비자
# (`from . import kiwoom_fields as K` → `K.split_venue`)를 위해 재수출한다.
from .kiwoom_venue import VENUE_SUFFIX, apply_venue, split_venue  # noqa: F401

# 실시간 타입 코드 (키움 REAL row의 "type").
TYPE_TRADE = "0B"  # 주식체결 → SnapshotKind.TRADE
TYPE_ORDERBOOK = "0D"  # 주식호가잔량 → SnapshotKind.OB
TYPE_MEMBER = "0F"  # 주식당일거래원 → SnapshotKind.BROKER
TYPE_PROGRAM = "0w"  # 종목프로그램매매 → 프로그램 사이드카 latch (PR-F4)
# 주식시간외호가 → SnapshotKind.AFTER_HOURS. **0D 의 시간외판이 아니다** — 아래 FID
# 절이 말하듯 10단 사다리가 없고 총잔량 두 개뿐이라, 별도 kind 로 나른다.
TYPE_AFTER_HOURS = "0E"
# 주식예상체결 → SnapshotKind.EXPECTED. **0D FID 23/24 의 대체가 아니라 보완**이다:
# 0D 는 15:30 에 끊기므로 시간외 단일가(16:00–18:00)의 10분 주기 단일가에는 예상체결
# 신호가 하나도 없었다. 그 구간의 REST(ka10087·ka10004)에도 예상체결 필드가 없고
# (2026-08-18 전체 키 실측), ka10007 의 `exp_cntr_*` 는 **정규장 잔상**이다 — 같은 날
# 16:30·16:40 두 체결 주기에 걸쳐 시간외 누적체결량이 1,173→1,915→2,562 로 늘었는데도
# 그 두 필드는 고유값 1 로 미동이었다(27표본). 그래서 WS 로만 얻을 수 있다.
#
# ⚠ **이 구간에 0H 가 실제로 흐르는지는 미실측이다.** 0B·0D·0E 는 전부 16:00 이후
# KRX 침묵이 실측됐고(2026-08-18 16:23 링버퍼: after_hours 0건), 0H 도 침묵일 개연성이
# 있다. 구독 비용이 0 이라(키움 한도는 종목 단위, 타입 무관) 넣어 두고 다음 거래일에
# 관측한다 — 0E 가 들어온 경로와 같다.
TYPE_EXPECTED = "0H"
# VI발동/해제 — **시장 전체 구독**(REG item=[""], 종목 슬롯 무관)이라 종목 파이프라인이
# 아니라 관측 캡처(kiwoom_vi_capture) 전용이다. 9068 발동구분·9069 발동방향·9075
# 장전구분의 코드값 legend 가 어떤 문서에도 없어 실이벤트 raw 채록으로만 확정 가능 —
# legend 확정 전까지 파서(kiwoom_frames)는 이 타입을 소비하지 않는다.
TYPE_VI = "1h"
# 업종지수(0J)·업종등락(0U) — VI 와 같은 **시장 관측** 부류다. 종목 파이프라인이
# 아니라 `/market` 표면(지수 카드·등락종목수·업종 온도)의 실시간 오버레이가 소비한다.
#
# **슬롯을 먹는다 — 코드당 1개, 타입 수 무관**(2026-08-07 실측: 업종 66개 등록 후
# 종목이 130 에서 `rc=105115` 로 막혔다). VI(`1h`)만 `item=[""]` 라 진짜 공짜다.
# 처음엔 빈 세션 프로브를 근거로 "무료" 라 적었는데, 운영에서 저장셋 199/200 계정에
# 얹으려다 거부되며 `tick_count: 0` 의 **조용한 실패**가 났다. 그래서 `coverage`
# 가 마지막 계정에 `KIWOOM_SECTOR_RESERVE` 만큼 자리를 예약한다.
#
# REG 의 `item` 은 **ka20003 과 같은 3자리 업종코드**다(공식 예제 `["001"]`, 실측으로
# 001·101·201·150·603 전부 틱 확인). `_NX`/`_AL` 거래소 접미는 이 두 타입에 없다.
TYPE_SECTOR_INDEX = "0J"
TYPE_SECTOR_UPDOWN = "0U"

# --- 0J 업종지수 FID (2026-08-07 실측 + 공식 필드표) ---
# ⚠ **레벨에도 등락 방향 부호가 접두로 붙는다**: 실측 `"10": "-1327.93"` ·
# `"16": "+1383.62"`. 레벨(10·16·17·18)은 abs 로 벗기고, 등락(11·12)은 부호가 곧
# 값이라 그대로 읽는다. ka20003 이 같은 함정으로 하락 업종 32개를 음수로 흘렸다.
SEC_TIME = "20"  # 체결시간 HHMMSS
SEC_PRICE = "10"  # 현재지수, 부호=등락방향
SEC_DELTA = "11"  # 전일대비, 부호 유의미
SEC_CHANGE_PCT = "12"  # 등락률 %, 부호 유의미
SEC_CUM_VOLUME = "13"  # 누적거래량
SEC_CUM_VALUE = "14"  # 누적거래대금 — ka20003 `trde_prica` 와 같은 **백만원** 축
SEC_OPEN = "16"  # 시가, 부호=등락방향
SEC_HIGH = "17"  # 고가, 부호=등락방향
SEC_LOW = "18"  # 저가, 부호=등락방향

# --- 0U 업종등락 FID ---
# 등락종목수가 여기 있다. 지수값(10·11·12)·거래대금(13·14)도 함께 오므로 0U 하나로
# ka20003 종합행의 대부분이 덮인다. `603`(변동성지수)엔 0U 틱이 오지 않는다 —
# 등락종목수 개념이 없는 지수라 정상이다(실측 18초 창에서 0틱).
SEC_UP_COUNT = "252"  # 상승종목수
SEC_UPPER_COUNT = "251"  # 상한종목수
SEC_FLAT_COUNT = "253"  # 보합종목수
SEC_DOWN_COUNT = "255"  # 하락종목수
SEC_LOWER_COUNT = "254"  # 하한종목수
SEC_TRADED_COUNT = "256"  # 거래형성종목수
SEC_TRADED_PCT = "257"  # 거래형성비율 %

# --- 1h VI발동/해제 FID (요약 로그용 — 저장은 raw 전체) ---
VI_CODE = "9001"  # 종목코드
VI_TRIGGER_PRICE = "1221"  # 발동가격
VI_KIND = "1225"  # 적용구분 — 공식 예제상 "정적" 한글 리터럴(코드값 아님)
VI_TRIGGER_DIR = "9069"  # 발동방향 — legend 미해독(캡처 목적 필드)

# --- 0D 주식호가잔량 FID ---
# 실측 확인(2026-07-16): 41-50 매도호가 오름차순(best=41), 51-60 매수호가 내림차순(best=51),
# 61-70 매도잔량, 71-80 매수잔량, 121/125 총잔량. 애프터마켓 단일가엔 3호가만 채워지고
# 4~10단계는 "-0"(=0)으로 온다 — abs(int("-0"))=0이라 빈 단계가 자연히 0으로 매핑된다.
OB_TIME = "21"  # 호가시간 HHMMSS
OB_ASK_PRICE = [str(f) for f in range(41, 51)]  # 매도호가 1~10
OB_BID_PRICE = [str(f) for f in range(51, 61)]  # 매수호가 1~10
OB_ASK_QTY = [str(f) for f in range(61, 71)]  # 매도잔량 1~10
OB_BID_QTY = [str(f) for f in range(71, 81)]  # 매수잔량 1~10
OB_TOTAL_ASK_QTY = "121"
OB_TOTAL_BID_QTY = "125"
# 예상체결가/예상체결량 — 동시호가(단일가) 개념. 파서가 **venue 별 시각 게이트
# (session_gate.is_auction_window) AND 값>0** 일 때만 payload 에 싣는다. 창이 venue
# 마다 다르다: KRX 08:30–09:00·15:20–15:30 / NXT 15:30–15:40(애프터마켓 시가단일가)
# / UN(`_AL`) 셋 전부. 값>0 만으론 부족한 이유는 NXT 가 접속매매 중에도 예상체결값을
# 흘리기 때문이다(2026-07-22 실측) — 근거는 `session_gate` 창 표와
# `kiwoom_frames._parse_orderbook` 주석에 있다.
# 200/201(예상체결대비/등락률)은 미소비.
OB_EXPECTED_PRICE = "23"  # 예상체결가, 부호=등락방향(abs)
OB_EXPECTED_QTY = "24"  # 예상체결량

# --- 0E 주식시간외호가 FID ---
#
# **왜 이 타입이 필요한가 — 15:30 에 호가가 통째로 멎는다.**
#
# 실측(2026-08-14, 사용자 dev 서버 표시 링 버퍼): KRX-only 종목(006360 GS건설 ·
# 069500 KODEX200)의 0D 는 15:30:25 · 15:30:22 를 끝으로 **0건**이 되는데, 0B 는
# 15:40:00 부터 다시 흐른다(각 24 · 115 건, 체결가 전량이 당일 종가 단일가).
# 즉 시간외 종가매매는 실제로 일어나는데 호가 축만 화면에서 죽어 있었다.
# NXT 상장 종목은 같은 시각 NXT·UN venue 로 0D 가 살아 있어 **KRX-only 종목만**
# 이 증상을 본다 — 그 비대칭이 이 타입을 붙인 계기다.
#
# **10단 사다리는 여기 없다 — 원리적으로.** 0E 가 싣는 FID 는 아래 다섯이 전부이고
# (키움 공식 `kiwoom_docs/실시간시세.md`), 0D 의 41~50(매도호가)·61~70(잔량) 같은
# 단계별 필드가 존재하지 않는다. 시장 구조와도 정합한다: 장후 시간외 종가매매는
# 당일 종가 **단일 가격**에 시간우선으로 체결되므로 호가 사다리라는 개념 자체가 없다.
# 그래서 소비자는 이 타입으로 사다리를 채우려 하면 안 된다 — 총잔량 두 개가 전부다.
#
# 미소비: 132 · 136(총잔량 직전대비). 소비처가 생기면 그때 싣는다.
# ── 0H 주식예상체결 ──────────────────────────────────────────────────────────
# FID 번호가 0B(체결)와 같다 — 벤더가 같은 슬롯에 **예상**값을 싣는다. 별도 이름을
# 두는 이유는 의미가 다르기 때문이다: 여기 10 은 체결가가 아니라 **예상체결가**이고,
# 15 는 체결량이 아니라 **예상체결량**이다. CNT_* 를 재사용하면 파서를 읽는 사람이
# 실체결로 오해한다.
#
# 11/12(전일대비·등락율)는 **미소비** — 0D 의 200/201 과 같은 규율이다. 이 구간에서
# 그 값의 분모가 전일종가인지 당일종가인지 미실측이라(ka10087 은 종가 기준이었다)
# 잘못 쓰면 등락률이 조용히 틀린다. 필요해지면 그때 실측하고 연다.
EXP_TIME = "20"  # 체결시간 HHMMSS (0B 의 CNT_TIME 과 같은 FID 번호)
EXP_PRICE = "10"  # 예상체결가, 부호=등락방향(abs)
EXP_QTY = "15"  # 예상체결량

AH_TIME = "21"  # 호가시간 HHMMSS (0D 의 OB_TIME 과 같은 FID 번호)
AH_TOTAL_ASK_QTY = "131"  # 시간외매도호가총잔량
AH_TOTAL_BID_QTY = "135"  # 시간외매수호가총잔량

# --- 0B 주식체결 FID ---
# FID 15(체결량)의 부호가 체결 방향: +매수 / -매도 / 0(동시호가 등). KIS side(1/5→±1)와
# 동형. FID 10(현재가)의 부호는 등락방향이라 가격은 abs로 크기만 취한다.
# NOTE: FID 15 부호=체결방향 규약은 정규장 스모크(계획 §6)에서 최종 확인 대상.
CNT_TIME = "20"  # 체결시간 HHMMSS
CNT_PRICE = "10"  # 현재가(체결가), 부호=등락방향
CNT_QTY = "15"  # 체결량, 부호=체결방향(+매수/-매도)
# 전일대비(부호 유의미 — abs 금지). 전일종가 = abs(CNT_PRICE) - CNT_DELTA 로 유도해
# 리스트 등락률의 기준가를 키움만으로 얻는다(KIS 폴링 의존 제거). 실측 검증:
# 2026-07-20 정규장 0B 6,880프레임 전건에 존재했고, 역산 전일종가가 KIS
# previous_close 와 5종목 전부 일치했다(tests/fixtures/kiwoom_t2/).
# FID 12(등락률)는 키움이 소수 2자리로 이미 반올림한 문자열이라 쓰지 않는다 —
# 10/11 에서 직접 계산해야 정밀도·반올림 규칙을 우리가 통제한다.
CNT_DELTA = "11"  # 전일대비(원), 부호=등락방향
# 당일 시가/고가/저가. 부호는 등락방향이라 _price 로 abs 만 취한다(CNT_PRICE 와 동형).
# 폴링(KIS 멀티시세 inter2_oprc/hgpr/lwpr)이 주던 값과 같은 의미 — 히트맵 행이 쓴다.
# 실측(2026-07-20 000660): 시가 1,745,000 · 고가 1,892,000 · 저가 1,735,000 로
# 저가 ≤ 현재가 ≤ 고가 관계가 성립했다.
CNT_OPEN = "16"
CNT_HIGH = "17"
CNT_LOW = "18"
# 종목 요약 지표 — 0B 43개 FID 중 그동안 버리던 것들. 이미 수신 중이라 슬롯·유량
# 비용이 0 이다(등록 단위는 종목이고 타입 무관 — kiwoom-ws-limits-measured).
# 의미는 골든 프레임 3종목 전건 산술 검산 + 키움 공식 GitHub 필드표 이중 확정:
#   228 = 1031(매수체결량)/1030(매도체결량)×100  → 100.61·120.42·94.41 소수 2자리 일치
#   30  = 13/(13−26)×100 (오늘 누적 ÷ 전일 전량)  → 43.85·37.24·32.53, 부호는 증감방향
#   620 = 14×1e6/13                              → 오차 0.0001% (14 가 백만원임을 이중 증명)
# ⚠️ 금액 단위가 한 프레임 안에서 3종 혼재한다: 14=백만원 · 29=원 · 1313=천원.
#    키움은 어떤 FID 에도 단위를 문서화하지 않으므로 새 금액 FID 소비 시 반드시 검산할 것.
CNT_FILL_STRENGTH = "228"  # 체결강도 %, 소수 2자리
CNT_VS_PREV_VOL_PCT = "30"  # 전일거래량 대비 비율 %, 부호=증감방향(크기만 소비)
CNT_CUM_VOLUME = "13"  # 누적거래량, 단위 1주
CNT_CUM_VALUE = "14"  # 누적거래대금, **백만원** — 검산 620=14×1e6/13 으로 단위 이중 증명
CNT_VWAP = "620"  # 당일거래평균가, 원

# --- 0F 주식당일거래원 FID ---
# 실측 확정(2026-07-20 T2 채록, docs/research/2026-07-20-kiwoom-0f-0w-t2-capture.md):
# 5쌍 구조 — 이름 141-145(매도)/151-155(매수), 당일 누적수량 161-165/171-175.
# 이름은 한글 텍스트로 내부 공백이 유의미하다("삼  성") — 양끝만 strip.
# 시각 FID 가 없어 t_ms 는 수신 시각(now_ms)을 쓴다(ADR-0111 REST 합성 경로와 동일 규약).
# 미소비: 146-160(회원사코드)·166-180(증감)·261-268(외국계 합 추정)·271-285(외국계,
# 국내사는 "!!!!" 센티넬)·337(갱신구분 추정) — 의미 미확정이거나 표시 요구 없음
# (플랜 2026-07-20 §PR-F1: BROKER 계약 shape-compat 최소만).
MEM_SELL_NAME = [str(f) for f in range(141, 146)]  # 매도 거래원 이름 1~5위
MEM_BUY_NAME = [str(f) for f in range(151, 156)]  # 매수 거래원 이름 1~5위
MEM_SELL_QTY = [str(f) for f in range(161, 166)]  # 매도 누적수량 1~5위
MEM_BUY_QTY = [str(f) for f in range(171, 176)]  # 매수 누적수량 1~5위

# --- 0w 종목프로그램매매 FID ---
# 실측 확정(2026-07-20 F3 검산 — 원본 205건 시계열, research 문서 §0w 표 참조):
# 순매수 산식 210=206−202·212=208−204 각 205/205 성립, 수량×주가≈금액 교차검증.
# **금액(204/208/212)은 백만원 단위** — KIS REST(원 단위)와 스케일이 달라 소비 전
# ×1,000,000 정규화 필수(EOD 실측: REST 399,887,415,500원 vs 0w 397,701백만원).
# 211/213(순매수 증감, 이벤트분)은 재전송·유실에 취약해 미소비 — 증감이 필요하면
# 누적 필드의 flush 간 diff 로 파생한다(F3 권고).
# 시각 FID 없음 → 수신 시각 사용(0F 와 동일 규약).
PRG_SELL_QTY = "202"  # 프로그램 매도수량 (누적, 주)
PRG_SELL_AMT = "204"  # 프로그램 매도금액 (누적, 백만원)
PRG_BUY_QTY = "206"  # 프로그램 매수수량 (누적, 주)
PRG_BUY_AMT = "208"  # 프로그램 매수금액 (누적, 백만원)
PRG_NET_QTY = "210"  # 순매수수량 (= 206-202)
PRG_NET_AMT = "212"  # 순매수금액 (= 208-204, 백만원)
