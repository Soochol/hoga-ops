---
scope: both
---

# 종목 스크리너 (Screener) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 전 종목 일봉 코퍼스를 거래대금·신고가(N,M)·신고거래량(N,M) 조건으로 거르는 `/screener` 페이지를 만들고, 결과 클릭 시 `/live` 호가 차트로 점프하게 한다.

**Architecture:** dev-tradingview TimescaleDB의 27년 일봉을 parquet으로 1회 시드(원주가 아카이브) → 오프라인 split 보정 + KIS 수정주가 교차검증으로 수정주가 store 파생 → DuckDB 윈도우 SQL로 sub-second 스캔. 일일 갱신은 gap 따라잡기(자동 EOD + 서버시작 복구 + 수동). 저장은 parquet, 쿼리는 기존 `QueryEngine`과 같은 in-memory DuckDB.

**Tech Stack:** Python/FastAPI, DuckDB(이미 dep), polars(이미 dep), pyarrow, pydantic; React/TypeScript, @tanstack/react-query, react-router; KIS Open API(`hoga/live/kis_client.py`); 데이터 소스 시드 = TimescaleDB(docker `tradingview-db`).

**Spec:** `docs/superpowers/specs/2026-05-31-screener-design.md`

---

## File Structure

**Backend (`hoga/api/`):**
- `models.py` (modify) — `Breakout`(판별 union), `ScreenerRow`, `ScreenerResponse`, `ScreenerStatusFile`, `NewHighFilter` 추가.
- `screener_store.py` (create) — seed/derive/update. 디스크 I/O + KIS fetch + polars 보정. parquet 경로 소유.
- `screener_scan.py` (create) — `run_scan(filters)`: DuckDB 윈도우 SQL. 순수 읽기, parquet→ScreenerRow.
- `screener.py` (create) — `build_router()`: `GET /api/screener`, `POST /api/screener/update`, `GET /api/screener/status`.
- `app.py` (modify) — `build_screener_router` 마운트, lifespan에서 시드 상태 확인.
- `scheduler.py` (modify) — `_daily_run`에 screener gap 단계 + 서버시작 복구.

**Backend (`hoga/live/`):**
- `kis_client.py` (modify) — `fetch_past_daily_candles`에 `adjust: bool` 인자(기본 수정주가 유지).
- `lifecycle.py` (modify) — `KisClient`를 프로세스 싱글턴으로(`ensure_kis_client`).

**Frontend (`frontend/src/`):**
- `api/screener.ts` (create) — 타입 + fetch 함수.
- `screener/useScreener.ts`, `screener/useScreenerStatus.ts` (create) — react-query 훅.
- `screener/ConditionPanel.tsx`, `screener/ResultTable.tsx`, `screener/StalenessChip.tsx`, `screener/BreakoutBadge.tsx` (create).
- `pages/Screener.tsx` (create) — 페이지 셸.
- `main.tsx`, `nav/LeftNav.tsx` (modify) — 라우트 + 네비.

**Data dir:** `<data_dir>/screener/{daily_unadjusted.parquet, daily_adjusted.parquet, stocks.parquet, status.json}`.

---

## Phase 1 — Backend data pipeline

### Task 1: Wire models

**Files:**
- Modify: `hoga/api/models.py`
- Test: `tests/api/test_screener_models.py`

- [ ] **Step 1: Write the failing test**

```python
# tests/api/test_screener_models.py
import pytest
from pydantic import ValidationError
from hoga.api.models import (
    BreakoutHit, BreakoutMiss, ScreenerRow, ScreenerResponse,
    ScreenerStatusFile, NewHighFilter,
)


def test_breakout_hit_requires_fields():
    h = BreakoutHit(hit=True, event_date="20260514", days_ago=3, period_extreme=323000)
    assert h.hit is True and h.days_ago == 3


def test_breakout_miss_is_bare():
    m = BreakoutMiss(hit=False)
    assert m.hit is False


def test_screener_row_code_is_str_leading_zero():
    row = ScreenerRow(
        code="005930", name="삼성전자", market="KOSPI", price=317000,
        trade_value_won=11699639541368, change_pct=5.84,
        new_high=BreakoutHit(hit=True, event_date="20260514", days_ago=0, period_extreme=323000),
        new_high_vol=BreakoutMiss(hit=False),
    )
    assert row.code == "005930"  # not int 5930


def test_new_high_filter_bounds():
    NewHighFilter(lookback=200, period=500)  # period>lookback ok
    with pytest.raises(ValidationError):
        NewHighFilter(lookback=0, period=500)  # lookback>=1


def test_screener_response_status_discriminates_empty():
    r = ScreenerResponse(status="not_seeded", rows=[])
    assert r.status == "not_seeded"


def test_status_file_schema_version():
    s = ScreenerStatusFile(
        schema_version=1, last_raw_date="20260514",
        last_built_ms=1, universe_size=3561, derive_ms=2,
    )
    assert s.schema_version == 1
```

- [ ] **Step 2: Run test to verify it fails**

Run: `uv run --extra dev pytest tests/api/test_screener_models.py -v`
Expected: FAIL with ImportError (models not defined).

- [ ] **Step 3: Write minimal implementation**

```python
# hoga/api/models.py — append near SymbolHit
from typing import Annotated, Literal, Union
from pydantic import BaseModel, Field

class BreakoutHit(BaseModel):
    """Lookback Window 안에서 Record Period 신고가 발생했음."""
    hit: Literal[True] = True
    event_date: str = Field(pattern=r"^\d{8}$")  # YYYYMMDD, 가장 최근 돌파일
    days_ago: int = Field(ge=0)                   # 거래일 기준
    period_extreme: int                            # 기간 최고가(신고가) 또는 최대거래량(신고거래량)

class BreakoutMiss(BaseModel):
    hit: Literal[False] = False

Breakout = Annotated[Union[BreakoutHit, BreakoutMiss], Field(discriminator="hit")]

class ScreenerRow(BaseModel):
    code: str                                      # 6자리 문자열, leading-zero 유지
    name: str
    market: Literal["KOSPI", "KOSDAQ"]
    price: int                                     # 최신 종가(원)
    trade_value_won: int                           # close*volume(원)
    change_pct: float | None                       # 전일대비등락률(%), 첫 상장일 None
    new_high: Breakout | None                      # 필터 off면 None
    new_high_vol: Breakout | None

class ScreenerResponse(BaseModel):
    status: Literal["ok", "not_seeded", "building"]
    rows: list[ScreenerRow]

class NewHighFilter(BaseModel):
    lookback: int = Field(ge=1)                    # N: Lookback Window
    period: int = Field(ge=1)                      # M: Record Period

class ScreenerStatusFile(BaseModel):
    """디스크 status.json (Stock-Date meta.json 과 별개)."""
    schema_version: int = 1
    last_raw_date: str = Field(pattern=r"^\d{8}$")
    last_built_ms: int
    universe_size: int
    derive_ms: int
```

- [ ] **Step 4: Run test to verify it passes**

Run: `uv run --extra dev pytest tests/api/test_screener_models.py -v`
Expected: PASS (6 passed).

- [ ] **Step 5: Commit**

```bash
git add hoga/api/models.py tests/api/test_screener_models.py
git commit -m "feat(screener): wire models (Breakout union, ScreenerRow, status file)"
```

---

### Task 2: Seed from DB → parquet (Code VARCHAR)

**Files:**
- Create: `hoga/api/screener_store.py`
- Test: `tests/api/test_screener_seed.py`

설계 노트: 시드는 두 단계 — (1) `docker exec ... psql \copy ... TO STDOUT CSV` 로 CSV 생성(외부), (2) `read_csv` 의 `types={'code':'VARCHAR'}` 로 parquet 변환. 테스트는 (2)만 검증한다(작은 CSV fixture). 실제 DB 시드는 `seed_from_db_csv(csv_path, out_path)` 가 받는 CSV 경로로 분리해 DB 비의존 테스트.

- [ ] **Step 1: Write the failing test**

```python
# tests/api/test_screener_seed.py
import duckdb
from pathlib import Path
from hoga.api.screener_store import seed_daily_from_csv


def test_seed_preserves_leading_zero_code(tmp_path: Path):
    csv = tmp_path / "in.csv"
    csv.write_text("code,date,open,high,low,close,volume\n"
                   "005930,2026-05-14,300,320,295,317,1000\n"
                   "000660,2026-05-14,2300,2350,2280,2333,500\n")
    out = tmp_path / "daily_unadjusted.parquet"
    n = seed_daily_from_csv(csv, out)
    assert n == 2
    rows = duckdb.sql(f"SELECT code, typeof(code) t FROM '{out}' ORDER BY code").fetchall()
    assert rows[0][0] == "000660" and rows[0][1] == "VARCHAR"
    assert rows[1][0] == "005930"  # not 5930
```

- [ ] **Step 2: Run test to verify it fails**

Run: `uv run --extra dev pytest tests/api/test_screener_seed.py -v`
Expected: FAIL ImportError.

- [ ] **Step 3: Write minimal implementation**

```python
# hoga/api/screener_store.py
from __future__ import annotations
from pathlib import Path
import duckdb

# parquet 컬럼 dtype 계약 (spec). code 는 전 구간 VARCHAR.
_DAILY_COLS = {
    "code": "VARCHAR", "date": "DATE", "open": "DOUBLE", "high": "DOUBLE",
    "low": "DOUBLE", "close": "DOUBLE", "volume": "BIGINT",
}


def seed_daily_from_csv(csv_path: Path, out_path: Path) -> int:
    """CSV(원주가 일봉) → daily_unadjusted.parquet. code VARCHAR 강제, count 반환."""
    out_path.parent.mkdir(parents=True, exist_ok=True)
    con = duckdb.connect(":memory:")
    con.execute(
        "COPY (SELECT code, CAST(date AS DATE) date, open, high, low, close, volume "
        "FROM read_csv(?, header=true, types={'code':'VARCHAR'})) "
        "TO ? (FORMAT parquet, COMPRESSION zstd)",
        [str(csv_path), str(out_path)],
    )
    return con.execute(f"SELECT count(*) FROM '{out_path}'").fetchone()[0]
```

- [ ] **Step 4: Run test to verify it passes**

Run: `uv run --extra dev pytest tests/api/test_screener_seed.py -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add hoga/api/screener_store.py tests/api/test_screener_seed.py
git commit -m "feat(screener): seed daily CSV->parquet with VARCHAR code"
```

- [ ] **Step 6: Add the DB-export driver (no unit test — integration helper)**

```python
# hoga/api/screener_store.py — append
import subprocess

def export_db_to_csv(csv_path: Path, *, container="tradingview-db",
                     db="tradingview", user="tradingview") -> None:
    """docker exec psql \\copy ohlcv_daily → CSV (운영 1회 시드용)."""
    sql = ("\\copy (SELECT code, date, open, high, low, close, volume "
           "FROM ohlcv_daily ORDER BY code, date) TO STDOUT WITH CSV HEADER")
    with csv_path.open("wb") as f:
        subprocess.run(["docker", "exec", container, "psql", "-U", user, "-d", db, "-c", sql],
                       stdout=f, check=True)
```

- [ ] **Step 7: Commit**

```bash
git add hoga/api/screener_store.py
git commit -m "feat(screener): db->csv export driver for one-time seed"
```

---

### Task 3: Offline split adjustment

**Files:**
- Modify: `hoga/api/screener_store.py`
- Test: `tests/api/test_screener_adjust.py`

알고리즘: 종목별 date ASC 정렬. 연속 두 날의 `close[d]/close[d-1]` 비율이 깨끗한 분할 분수(1/2,1/3,1/4,1/5,1/10,1/50 및 역수)에 ±3% 이내면 분할로 판정. 분할일 d 이전(<d)의 OHLC 에 누적계수(factor)를 곱하고 volume 은 나눈다(back-adjust, 최신일 basis). polars 로 per-code 처리.

- [ ] **Step 1: Write the failing test**

```python
# tests/api/test_screener_adjust.py
import polars as pl
from hoga.api.screener_store import adjust_splits


def test_5to1_split_backadjusts_history():
    # 분할 전 500, 분할일 100(5:1). 보정 후 분할 전이 100으로 내려와야 연속.
    df = pl.DataFrame({
        "code": ["A"] * 3,
        "date": ["2021-04-13", "2021-04-14", "2021-04-15"],
        "open": [500.0, 500.0, 100.0], "high": [510.0, 510.0, 102.0],
        "low": [495.0, 495.0, 99.0], "close": [500.0, 500.0, 100.0],
        "volume": [1000, 1000, 5000],
    }).with_columns(pl.col("date").str.to_date())
    out = adjust_splits(df).sort("date")
    closes = out["close"].to_list()
    # 분할 전 두 날이 1/5로 내려와 100 근처, 마지막은 그대로 100
    assert abs(closes[0] - 100.0) < 1.0
    assert abs(closes[2] - 100.0) < 1.0
    # 거래량은 분할 전이 5배로 올라가 연속(1000*5)
    assert out.sort("date")["volume"].to_list()[0] == 5000


def test_no_split_is_identity():
    df = pl.DataFrame({
        "code": ["B"] * 2, "date": ["2026-05-13", "2026-05-14"],
        "open": [300.0, 305.0], "high": [310.0, 312.0],
        "low": [298.0, 303.0], "close": [305.0, 310.0], "volume": [100, 120],
    }).with_columns(pl.col("date").str.to_date())
    out = adjust_splits(df).sort("date")
    assert out["close"].to_list() == [305.0, 310.0]
```

- [ ] **Step 2: Run to verify it fails**

Run: `uv run --extra dev pytest tests/api/test_screener_adjust.py -v`
Expected: FAIL ImportError.

- [ ] **Step 3: Write minimal implementation**

```python
# hoga/api/screener_store.py — append
import polars as pl

# 깨끗한 분할 비율(신주/구주) 후보와 역수. ratio = close[d]/close[d-1].
_SPLIT_RATIOS = [1/2, 1/3, 1/4, 1/5, 1/10, 1/20, 1/50, 2, 3, 4, 5, 10]
_SPLIT_TOL = 0.03  # ±3%

def _detect_factor(ratio: float) -> float | None:
    """ratio 가 깨끗한 분할 비율에 가까우면 그 비율, 아니면 None."""
    for r in _SPLIT_RATIOS:
        if abs(ratio - r) / r <= _SPLIT_TOL:
            return r
    return None

def adjust_splits(df: pl.DataFrame) -> pl.DataFrame:
    """원주가 일봉 → 수정주가(최신일 basis). per-code back-adjust."""
    out = []
    for (code,), g in df.sort("date").group_by(["code"], maintain_order=True):
        g = g.sort("date")
        closes = g["close"].to_list()
        # 각 날 d 의 누적계수: d 이후 모든 분할의 비율 곱. 최신일 factor=1.
        n = len(closes)
        factor = [1.0] * n
        cum = 1.0
        for d in range(n - 1, 0, -1):
            ratio = closes[d] / closes[d - 1] if closes[d - 1] else 1.0
            split = _detect_factor(ratio)
            if split is not None:
                cum *= split            # d 시점에 split 발생 → d 이전에 split 곱
            factor[d - 1] = cum
        f = pl.Series("f", factor)
        adj = g.with_columns([
            (pl.col(c) * f).alias(c) for c in ("open", "high", "low", "close")
        ]).with_columns((pl.col("volume") / f).cast(pl.Int64).alias("volume"))
        out.append(adj)
    return pl.concat(out)
```

- [ ] **Step 4: Run to verify it passes**

Run: `uv run --extra dev pytest tests/api/test_screener_adjust.py -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add hoga/api/screener_store.py tests/api/test_screener_adjust.py
git commit -m "feat(screener): offline split back-adjustment (보정 A)"
```

---

### Task 4: derive_adjusted (parquet → parquet)

**Files:**
- Modify: `hoga/api/screener_store.py`
- Test: `tests/api/test_screener_derive.py`

KIS 교차검증·per-code 폴백은 KIS 의존이라 별도 함수 `crosscheck_suspect_codes`(backlog 표시, plan에서는 hook만)로 두고, 이 태스크는 `derive_adjusted(unadjusted_path, out_path)` = 읽기→adjust_splits→쓰기만 구현.

- [ ] **Step 1: Write the failing test**

```python
# tests/api/test_screener_derive.py
import polars as pl
from pathlib import Path
from hoga.api.screener_store import derive_adjusted


def test_derive_writes_adjusted(tmp_path: Path):
    raw = tmp_path / "u.parquet"
    pl.DataFrame({
        "code": ["A","A","A"], "date": ["2021-04-13","2021-04-14","2021-04-15"],
        "open":[500.0,500.0,100.0],"high":[510.0,510.0,102.0],
        "low":[495.0,495.0,99.0],"close":[500.0,500.0,100.0],"volume":[1000,1000,5000],
    }).with_columns(pl.col("date").str.to_date()).write_parquet(raw)
    out = tmp_path / "a.parquet"
    ms = derive_adjusted(raw, out)
    assert ms >= 0 and out.exists()
    got = pl.read_parquet(out).sort("date")
    assert abs(got["close"].to_list()[0] - 100.0) < 1.0
```

- [ ] **Step 2: Run to verify it fails** — `uv run --extra dev pytest tests/api/test_screener_derive.py -v` → FAIL ImportError.

- [ ] **Step 3: Implement**

```python
# hoga/api/screener_store.py — append
import time

def derive_adjusted(unadjusted_path: Path, out_path: Path) -> int:
    """원주가 parquet → 수정주가 parquet. 소요 ms 반환."""
    t0 = time.perf_counter()
    df = pl.read_parquet(unadjusted_path)
    adjust_splits(df).write_parquet(out_path, compression="zstd")
    return int((time.perf_counter() - t0) * 1000)
```

- [ ] **Step 4: Run to verify it passes** — PASS.

- [ ] **Step 5: Commit**

```bash
git add hoga/api/screener_store.py tests/api/test_screener_derive.py
git commit -m "feat(screener): derive_adjusted parquet pipeline"
```

---

### Task 5: Scan (DuckDB window SQL)

**Files:**
- Create: `hoga/api/screener_scan.py`
- Test: `tests/api/test_screener_scan.py`

dev-tradingview `buildScanQuery` 윈도우 로직 이식. 입력 = 수정주가 parquet 경로 + 필터. 출력 = `list[ScreenerRow]`. lookback/period 는 거래일, 돌파 `>=`(동점), `wc=period` 가드.

- [ ] **Step 1: Write the failing test**

```python
# tests/api/test_screener_scan.py
import polars as pl
from pathlib import Path
from hoga.api.screener_scan import run_scan
from hoga.api.models import NewHighFilter


def _store(tmp_path, rows):
    # rows: list of (code, date 'YYYY-MM-DD', high, volume); close=high, others derived
    df = pl.DataFrame({
        "code":[r[0] for r in rows], "date":[r[1] for r in rows],
        "open":[r[2] for r in rows], "high":[r[2] for r in rows],
        "low":[r[2] for r in rows], "close":[r[2] for r in rows],
        "volume":[r[3] for r in rows],
    }).with_columns(pl.col("date").str.to_date())
    p = tmp_path / "a.parquet"; df.write_parquet(p)
    s = tmp_path / "stocks.parquet"
    pl.DataFrame({"code":["A"],"name":["에이"],"market":["KOSPI"],
                  "is_etf":[False],"is_halted":[False]}).write_parquet(s)
    return p, s


def test_new_high_hit_within_lookback(tmp_path: Path):
    # 5일: 마지막 날이 직전 4일 최고를 갱신 → period=4, lookback=1 매치
    rows = [("A","2026-05-10",100,10),("A","2026-05-11",110,10),
            ("A","2026-05-12",105,10),("A","2026-05-13",108,10),
            ("A","2026-05-14",120,10)]
    p, s = _store(tmp_path, rows)
    out = run_scan(p, s, new_high=NewHighFilter(lookback=1, period=4))
    assert len(out) == 1 and out[0].new_high.hit is True
    assert out[0].new_high.event_date == "20260514"


def test_partial_window_guard_excludes_short_history(tmp_path: Path):
    rows = [("A","2026-05-13",100,10),("A","2026-05-14",120,10)]  # 2일 < period 5
    p, s = _store(tmp_path, rows)
    out = run_scan(p, s, new_high=NewHighFilter(lookback=1, period=5))
    assert out == []


def test_trade_value_filter(tmp_path: Path):
    rows = [("A","2026-05-14",100,10)]  # close*vol=1000원
    p, s = _store(tmp_path, rows)
    assert run_scan(p, s, min_trade_value_eok=0.0) and not \
        run_scan(p, s, min_trade_value_eok=1.0)  # 1억 미달
```

- [ ] **Step 2: Run to verify it fails** — `uv run --extra dev pytest tests/api/test_screener_scan.py -v` → FAIL ImportError.

- [ ] **Step 3: Implement**

```python
# hoga/api/screener_scan.py
from __future__ import annotations
from pathlib import Path
import duckdb
from hoga.api.models import (
    NewHighFilter, ScreenerRow, BreakoutHit, BreakoutMiss,
)

def _breakout_cte(name: str, col: str, f: NewHighFilter) -> str:
    """col(high|volume) 의 (lookback,period) 돌파 이력 CTE. dev-tradingview 윈도우 이식."""
    N, M = f.lookback, f.period
    return f"""
    {name}_lb AS (
      SELECT code, MIN(date) lb_start FROM (
        SELECT code, date, ROW_NUMBER() OVER (PARTITION BY code ORDER BY date DESC) rn
        FROM adj) t WHERE rn <= {N} GROUP BY code),
    {name}_win AS (
      SELECT code, date, {col} AS v,
        MAX({col}) OVER (PARTITION BY code ORDER BY date
                         ROWS BETWEEN {M-1} PRECEDING AND CURRENT ROW) mx,
        COUNT(*) OVER (PARTITION BY code ORDER BY date
                      ROWS BETWEEN {M-1} PRECEDING AND CURRENT ROW) wc
      FROM adj),
    {name} AS (
      SELECT DISTINCT ON (w.code) w.code, w.date event_date, CAST(w.v AS BIGINT) period_extreme,
        (SELECT max(date) FROM adj a WHERE a.code=w.code) - w.date AS days_ago
      FROM {name}_win w JOIN {name}_lb l ON l.code=w.code
      WHERE w.date >= l.lb_start AND w.v >= w.mx AND w.wc = {M}
      ORDER BY w.code, w.date DESC)"""

def run_scan(adjusted_path: Path, stocks_path: Path, *,
             new_high: NewHighFilter | None = None,
             new_high_vol: NewHighFilter | None = None,
             min_trade_value_eok: float | None = None,
             markets: list[str] | None = None,
             exclude_etf: bool = False, exclude_halted: bool = False,
             q: str | None = None, limit: int = 1000) -> list[ScreenerRow]:
    con = duckdb.connect(":memory:")
    con.execute(f"CREATE VIEW adj AS SELECT * FROM '{adjusted_path}'")
    con.execute(f"CREATE VIEW stk AS SELECT * FROM '{stocks_path}'")

    ctes = ["base AS (SELECT DISTINCT ON (code) code, date, high, close, volume "
            "FROM adj ORDER BY code, date DESC)"]
    joins, wheres = [], []
    if new_high:
        ctes.append(_breakout_cte("nh", "high", new_high)); joins.append("nh")
    if new_high_vol:
        ctes.append(_breakout_cte("nhv", "volume", new_high_vol)); joins.append("nhv")

    sel = ["base.code", "stk.name", "stk.market", "base.close::BIGINT price",
           "(base.close*base.volume)::BIGINT trade_value_won"]
    sel += ["nh.event_date nh_date, nh.days_ago nh_days, nh.period_extreme nh_ext"
            if new_high else "NULL nh_date, NULL nh_days, NULL nh_ext"]
    sel += ["nhv.event_date nhv_date, nhv.days_ago nhv_days, nhv.period_extreme nhv_ext"
            if new_high_vol else "NULL nhv_date, NULL nhv_days, NULL nhv_ext"]

    join_sql = "JOIN stk ON stk.code=base.code "
    for j in joins:
        join_sql += f"JOIN {j} ON {j}.code=base.code "
    if min_trade_value_eok is not None:
        wheres.append(f"base.close*base.volume >= {min_trade_value_eok*1e8}")
    if markets:
        lit = ",".join(f"'{m}'" for m in markets); wheres.append(f"stk.market IN ({lit})")
    if exclude_etf:
        wheres.append("NOT stk.is_etf")
    if exclude_halted:
        wheres.append("NOT stk.is_halted")
    if q:
        wheres.append(f"(stk.name LIKE '%{q}%' OR base.code LIKE '{q}%')")
    where_sql = ("WHERE " + " AND ".join(wheres)) if wheres else ""

    sql = (f"WITH {', '.join(ctes)} SELECT {', '.join(sel)} FROM base {join_sql}"
           f"{where_sql} ORDER BY trade_value_won DESC LIMIT {limit}")
    cols = [c[0] for c in con.execute(sql).description]
    out: list[ScreenerRow] = []
    for r in con.execute(sql).fetchall():
        d = dict(zip(cols, r))
        def bk(date, days, ext):
            return (BreakoutHit(hit=True, event_date=date.strftime("%Y%m%d"),
                                days_ago=int(days), period_extreme=int(ext))
                    if date is not None else BreakoutMiss(hit=False))
        out.append(ScreenerRow(
            code=d["code"], name=d["name"], market=d["market"], price=int(d["price"]),
            trade_value_won=int(d["trade_value_won"]), change_pct=None,
            new_high=bk(d["nh_date"], d["nh_days"], d["nh_ext"]) if new_high else None,
            new_high_vol=bk(d["nhv_date"], d["nhv_days"], d["nhv_ext"]) if new_high_vol else None,
        ))
    return out
```

- [ ] **Step 4: Run to verify it passes** — PASS (3 tests). Note: `change_pct` 는 base 에 전일 종가 LAG 추가로 후속 채움(Task 9 enrich) — 이 태스크는 None.

- [ ] **Step 5: Commit**

```bash
git add hoga/api/screener_scan.py tests/api/test_screener_scan.py
git commit -m "feat(screener): DuckDB window scan (신고가/신고거래량 breakout)"
```

---

## Phase 2 — KIS plumbing

### Task 6: Parameterize fetch_past_daily_candles adjust flag

**Files:**
- Modify: `hoga/live/kis_client.py:600-635`
- Test: `tests/live/test_kis_daily_adjust_flag.py`

- [ ] **Step 1: Write the failing test** (transport mock 으로 params 검증)

```python
# tests/live/test_kis_daily_adjust_flag.py
import httpx, pytest
from pathlib import Path
from hoga.live.kis_client import KisClient, KisCredentials

class _Capture(httpx.AsyncBaseTransport):
    def __init__(self): self.last_params = None
    async def handle_async_request(self, req):
        self.last_params = dict(req.url.params)
        return httpx.Response(200, json={"rt_cd":"0","output1":{},"output2":[]})

@pytest.mark.asyncio
async def test_adjust_flag_default_is_0_and_overridable(tmp_path: Path):
    t = _Capture()
    c = KisClient(KisCredentials(app_key="k", app_secret="s"),
                  token_cache_path=tmp_path/"tok.json", _transport=t)
    c._token = "x"; from datetime import datetime, timedelta
    from hoga.live.kis_client import KIS_KST
    c._token_expires_at = datetime.now(KIS_KST)+timedelta(hours=1)
    await c.fetch_past_daily_candles("005930","20260101","20260131")
    assert t.last_params["FID_ORG_ADJ_PRC"] == "0"   # 기본 수정주가(/live 보존)
    await c.fetch_past_daily_candles("005930","20260101","20260131", adjust=False)
    assert t.last_params["FID_ORG_ADJ_PRC"] == "1"   # 원주가(스크리너)
```

- [ ] **Step 2: Run to verify it fails** — `uv run --extra dev pytest tests/live/test_kis_daily_adjust_flag.py -v` → FAIL (unexpected `adjust` kwarg).

- [ ] **Step 3: Implement** — `fetch_past_daily_candles` 시그니처에 `adjust: bool = True` 추가, params 에서 `"FID_ORG_ADJ_PRC": "0" if adjust else "1"`.

```python
# hoga/live/kis_client.py — fetch_past_daily_candles 시그니처/params
async def fetch_past_daily_candles(
    self, code: str, from_yyyymmdd: str, to_yyyymmdd: str, *, adjust: bool = True,
) -> DailyCandleFetchResult:
    ...
    params = {
        "FID_COND_MRKT_DIV_CODE": _STOCK_MRKT_DIV, "FID_INPUT_ISCD": code,
        "FID_INPUT_DATE_1": from_yyyymmdd, "FID_INPUT_DATE_2": cursor_to,
        "FID_PERIOD_DIV_CODE": "D",
        "FID_ORG_ADJ_PRC": "0" if adjust else "1",  # 0=수정주가(/live 기본), 1=원주가(스크리너)
    }
```

- [ ] **Step 4: Run to verify it passes** — PASS. 또한 `uv run --extra dev pytest tests/ -k daily -q` 로 /live 일봉 회귀 무사.

- [ ] **Step 5: Commit**

```bash
git add hoga/live/kis_client.py tests/live/test_kis_daily_adjust_flag.py
git commit -m "feat(kis): parameterize daily candle adjust flag (default preserves /live)"
```

---

### Task 7: KisClient process singleton

**Files:**
- Modify: `hoga/live/lifecycle.py:100-107`
- Test: `tests/live/test_kis_singleton.py`

폴러 소유 → 프로세스 소유. `ensure_kis_client(token_cache_path, creds)` = 없으면 생성·보존, 있으면 반환. 폴러 stop 은 더 이상 `set_kis_client(None)` 하지 않음(셧다운/`reset_for_tests` 만 해제).

- [ ] **Step 1: Write the failing test**

```python
# tests/live/test_kis_singleton.py
from pathlib import Path
from hoga.live import lifecycle
from hoga.live.kis_client import KisCredentials

def test_ensure_returns_same_instance(tmp_path: Path):
    lifecycle.reset_for_tests()
    creds = KisCredentials(app_key="k", app_secret="s")
    a = lifecycle.ensure_kis_client(tmp_path/"t.json", creds)
    b = lifecycle.ensure_kis_client(tmp_path/"t.json", creds)
    assert a is b  # 단일 인스턴스(단일 토큰버킷)
```

- [ ] **Step 2: Run to verify it fails** — FAIL (no `ensure_kis_client`).

- [ ] **Step 3: Implement**

```python
# hoga/live/lifecycle.py — near get_kis_client
def ensure_kis_client(token_cache_path, creds) -> KisClient:
    """프로세스 단일 KisClient 보장(폴러 on/off 무관, 단일 토큰버킷)."""
    global _kis_client
    if _kis_client is None:
        _kis_client = KisClient(credentials=creds, token_cache_path=token_cache_path)
    return _kis_client
```

그리고 폴러 stop 경로(`stop_live_poller` 등)에서 `set_kis_client(None)` 호출을 제거(있다면). `reset_for_tests` 의 `_kis_client = None` 은 유지.

- [ ] **Step 4: Run to verify it passes** — PASS. `uv run --extra dev pytest tests/live -q` 로 폴러 회귀 확인.

- [ ] **Step 5: Commit**

```bash
git add hoga/live/lifecycle.py tests/live/test_kis_singleton.py
git commit -m "feat(live): KisClient process singleton (shared token bucket)"
```

---

## Phase 3 — Update + API

### Task 8: Gap catch-up update

**Files:**
- Modify: `hoga/api/screener_store.py`
- Test: `tests/api/test_screener_update.py`

`compute_gap(unadjusted_path, trading_days)` = 아카이브 최신일 다음~오늘 사이 거래일. `apply_gap_rows(unadjusted_path, new_rows)` = append. `write_status(...)`. KIS fetch 는 호출자(Task 9/10)에서 주입(테스트 격리).

- [ ] **Step 1: Write the failing test**

```python
# tests/api/test_screener_update.py
import polars as pl, json
from pathlib import Path
from hoga.api.screener_store import last_raw_date, append_rows, write_status, read_status

def test_last_date_and_append(tmp_path: Path):
    p = tmp_path/"u.parquet"
    pl.DataFrame({"code":["A"],"date":["2026-05-13"],"open":[1.0],"high":[1.0],
                  "low":[1.0],"close":[1.0],"volume":[1]}).with_columns(
                  pl.col("date").str.to_date()).write_parquet(p)
    assert last_raw_date(p) == "20260513"
    append_rows(p, pl.DataFrame({"code":["A"],"date":["2026-05-14"],"open":[2.0],
        "high":[2.0],"low":[2.0],"close":[2.0],"volume":[2]}).with_columns(
        pl.col("date").str.to_date()))
    assert last_raw_date(p) == "20260514"

def test_status_roundtrip(tmp_path: Path):
    sp = tmp_path/"status.json"
    write_status(sp, last_raw_date="20260514", universe_size=2, derive_ms=3, now_ms=100)
    s = read_status(sp)
    assert s.last_raw_date == "20260514" and s.schema_version == 1
```

- [ ] **Step 2: Run to verify it fails** — FAIL ImportError.

- [ ] **Step 3: Implement**

```python
# hoga/api/screener_store.py — append
import json
from hoga.api.models import ScreenerStatusFile

def last_raw_date(unadjusted_path: Path) -> str | None:
    if not unadjusted_path.exists(): return None
    r = duckdb.connect(":memory:").execute(
        f"SELECT max(date) FROM '{unadjusted_path}'").fetchone()[0]
    return r.strftime("%Y%m%d") if r else None

def append_rows(unadjusted_path: Path, new: pl.DataFrame) -> None:
    base = pl.read_parquet(unadjusted_path)
    pl.concat([base, new.select(base.columns)]).write_parquet(
        unadjusted_path, compression="zstd")

def write_status(path: Path, *, last_raw_date: str, universe_size: int,
                 derive_ms: int, now_ms: int) -> None:
    path.write_text(ScreenerStatusFile(
        schema_version=1, last_raw_date=last_raw_date, last_built_ms=now_ms,
        universe_size=universe_size, derive_ms=derive_ms).model_dump_json())

def read_status(path: Path) -> ScreenerStatusFile | None:
    if not path.exists(): return None
    return ScreenerStatusFile.model_validate_json(path.read_text())
```

- [ ] **Step 4: Run to verify it passes** — PASS.

- [ ] **Step 5: Commit**

```bash
git add hoga/api/screener_store.py tests/api/test_screener_update.py
git commit -m "feat(screener): gap append + status.json read/write"
```

---

### Task 9: API routes

**Files:**
- Create: `hoga/api/screener.py`
- Modify: `hoga/api/app.py`
- Test: `tests/api/test_screener_routes.py`

`GET /api/screener` 는 store 경로(`data_dir/screener/*`)에서 scan. status.json 없으면 `not_seeded`. 필터 쌍(nh_lookback+nh_period) 둘 다-또는-없음 검증(422).

- [ ] **Step 1: Write the failing test**

```python
# tests/api/test_screener_routes.py
import polars as pl
from fastapi.testclient import TestClient
from fastapi import FastAPI
from hoga.api.screener import build_router

def _app(tmp_path):
    sd = tmp_path/"screener"; sd.mkdir()
    pl.DataFrame({"code":["A"],"date":["2026-05-14"],"open":[100.0],"high":[100.0],
        "low":[100.0],"close":[100.0],"volume":[10]}).with_columns(
        pl.col("date").str.to_date()).write_parquet(sd/"daily_adjusted.parquet")
    pl.DataFrame({"code":["A"],"name":["에이"],"market":["KOSPI"],
        "is_etf":[False],"is_halted":[False]}).write_parquet(sd/"stocks.parquet")
    (sd/"status.json").write_text('{"schema_version":1,"last_raw_date":"20260514",'
        '"last_built_ms":1,"universe_size":1,"derive_ms":1}')
    app = FastAPI(); app.include_router(build_router(data_dir=tmp_path)); return app

def test_scan_ok(tmp_path):
    c = TestClient(_app(tmp_path))
    r = c.get("/api/screener", params={"min_trade_value_eok":0})
    assert r.status_code == 200 and r.json()["status"] == "ok"
    assert r.json()["rows"][0]["code"] == "A"

def test_not_seeded(tmp_path):
    app = FastAPI(); app.include_router(build_router(data_dir=tmp_path))
    r = TestClient(app).get("/api/screener")
    assert r.json()["status"] == "not_seeded" and r.json()["rows"] == []

def test_filter_pair_required(tmp_path):
    r = TestClient(_app(tmp_path)).get("/api/screener", params={"nh_lookback":200})
    assert r.status_code == 422
```

- [ ] **Step 2: Run to verify it fails** — FAIL ImportError.

- [ ] **Step 3: Implement**

```python
# hoga/api/screener.py
from __future__ import annotations
from pathlib import Path
from fastapi import APIRouter, HTTPException, Query
from hoga.api.models import NewHighFilter, ScreenerResponse, ScreenerStatusFile
from hoga.api import screener_scan, screener_store

def build_router(*, data_dir: Path) -> APIRouter:
    router = APIRouter(prefix="/api/screener", tags=["screener"])
    sdir = data_dir / "screener"

    def _pair(lb, pd, name):
        if (lb is None) != (pd is None):
            raise HTTPException(422, f"{name}_lookback and {name}_period must be set together")
        return NewHighFilter(lookback=lb, period=pd) if lb is not None else None

    @router.get("")
    def scan(
        min_trade_value_eok: float | None = None,
        nh_lookback: int | None = None, nh_period: int | None = None,
        nhv_lookback: int | None = None, nhv_period: int | None = None,
        markets: list[str] | None = Query(None),
        exclude_etf: bool = False, exclude_halted: bool = False,
        q: str | None = None, limit: int = Query(1000, ge=1, le=2000),
    ) -> ScreenerResponse:
        if not (sdir / "status.json").exists():
            return ScreenerResponse(status="not_seeded", rows=[])
        rows = screener_scan.run_scan(
            sdir/"daily_adjusted.parquet", sdir/"stocks.parquet",
            new_high=_pair(nh_lookback, nh_period, "nh"),
            new_high_vol=_pair(nhv_lookback, nhv_period, "nhv"),
            min_trade_value_eok=min_trade_value_eok, markets=markets,
            exclude_etf=exclude_etf, exclude_halted=exclude_halted, q=q, limit=limit)
        return ScreenerResponse(status="ok", rows=rows)

    @router.get("/status")
    def status() -> dict:
        s = screener_store.read_status(sdir/"status.json")
        if s is None:
            return {"status": "not_seeded"}
        return {**s.model_dump(), "status": "ok"}

    return router
```

`app.py`: `from hoga.api.screener import build_router as build_screener_router` 후
`app.include_router(build_screener_router(data_dir=data_dir))`.

- [ ] **Step 4: Run to verify it passes** — PASS (3 tests).

- [ ] **Step 5: Commit**

```bash
git add hoga/api/screener.py hoga/api/app.py tests/api/test_screener_routes.py
git commit -m "feat(screener): API routes (scan/status, filter-pair validation)"
```

> POST /api/screener/update(gap 따라잡기 + KIS fetch + re-derive + status)와 scheduler hook(Task 10)은 KIS·스케줄러 의존이라 통합 단계에서. update 는 `ensure_kis_client` + `fetch_past_daily_candles(adjust=False)` + `append_rows` + `derive_adjusted` + `write_status`를 순차 호출하는 orchestration 으로, single-flight 는 `symbols._RefreshCoordinator` 패턴 재사용.

---

### Task 10: Scheduler hook + update endpoint

**Files:**
- Modify: `hoga/api/scheduler.py:40-71` (`_daily_run`), `hoga/api/screener.py`, `hoga/api/app.py` (startup recovery)
- Test: `tests/api/test_screener_scheduler.py`

- [ ] **Step 1: Write failing test** — `_daily_run` 이 screener gap 단계를 부르는지(주입된 fake updater 호출 검증), update endpoint 가 single-flight 인지.

```python
# tests/api/test_screener_scheduler.py
import asyncio, polars as pl
from pathlib import Path
from hoga.api import screener_store

def test_run_update_appends_and_derives(tmp_path, monkeypatch):
    sd = tmp_path/"screener"; sd.mkdir()
    pl.DataFrame({"code":["A"],"date":["2026-05-13"],"open":[1.0],"high":[1.0],
        "low":[1.0],"close":[1.0],"volume":[1]}).with_columns(
        pl.col("date").str.to_date()).write_parquet(sd/"daily_unadjusted.parquet")
    # fake KIS: 2026-05-14 한 줄
    def fake_fetch(code, frm, to):
        return pl.DataFrame({"code":[code],"date":["2026-05-14"],"open":[2.0],
            "high":[2.0],"low":[2.0],"close":[2.0],"volume":[2]}).with_columns(
            pl.col("date").str.to_date())
    n = screener_store.run_update(sd, codes=["A"], fetch_one=fake_fetch,
                                  trading_days=["20260514"], now_ms=100)
    assert n == 1
    assert (sd/"daily_adjusted.parquet").exists()
    assert screener_store.read_status(sd/"status.json").last_raw_date == "20260514"
```

- [ ] **Step 2: Run to verify it fails** — FAIL (no `run_update`).

- [ ] **Step 3: Implement** `screener_store.run_update(sdir, *, codes, fetch_one, trading_days, now_ms)`: gap 거래일에 대해 `fetch_one` 으로 종목별 원주가 행 수집 → `append_rows` → `derive_adjusted` → `write_status`. 그리고 `scheduler._daily_run` 에 screener 단계 추가(`ensure_kis_client` + `fetch_past_daily_candles(adjust=False)` 어댑터로 `fetch_one` 구성, `calendar.trading_days_in_range` 로 gap). `app.py` lifespan 에 startup recovery(서버 시작 시 1회 `run_update`). update endpoint(`POST /api/screener/update`)는 `_RefreshCoordinator` single-flight 로 같은 `run_update` 호출 + `EventBus.publish('screener.update', {done,total})`.

- [ ] **Step 4: Run to verify it passes** — PASS.

- [ ] **Step 5: Commit**

```bash
git add hoga/api/screener_store.py hoga/api/screener.py hoga/api/scheduler.py hoga/api/app.py tests/api/test_screener_scheduler.py
git commit -m "feat(screener): daily gap update (scheduler + startup recovery + manual endpoint)"
```

---

## Phase 4 — Frontend

### Task 11: API client + types

**Files:**
- Create: `frontend/src/api/screener.ts`
- Test: `frontend/src/api/screener.test.ts`

- [ ] **Step 1: Write failing test** (타입·URL 빌드 검증, `apiCall` mock)

```typescript
// frontend/src/api/screener.test.ts
import { describe, it, expect, vi } from 'vitest';
vi.mock('./client', () => ({ apiCall: vi.fn(async () => ({ status: 'ok', rows: [] })) }));
import { runScreener } from './screener';
import { apiCall } from './client';

describe('runScreener', () => {
  it('encodes filter pair', async () => {
    await runScreener({ newHigh: { lookback: 200, period: 500 }, minTradeValueEok: 50 });
    expect(apiCall).toHaveBeenCalledWith(expect.stringContaining('nh_lookback=200'));
    expect(apiCall).toHaveBeenCalledWith(expect.stringContaining('nh_period=500'));
  });
});
```

- [ ] **Step 2: Run to verify it fails** — `cd frontend && npx vitest run src/api/screener.test.ts` → FAIL.

- [ ] **Step 3: Implement**

```typescript
// frontend/src/api/screener.ts
import { apiCall } from './client';

export type Breakout =
  | { hit: true; event_date: string; days_ago: number; period_extreme: number }
  | { hit: false };
export interface ScreenerRow {
  code: string; name: string; market: 'KOSPI' | 'KOSDAQ';
  price: number; trade_value_won: number; change_pct: number | null;
  new_high: Breakout | null; new_high_vol: Breakout | null;
}
export interface ScreenerResponse { status: 'ok' | 'not_seeded' | 'building'; rows: ScreenerRow[]; }
export interface ScreenerStatus { status: string; last_raw_date?: string; universe_size?: number; }

export interface NMFilter { lookback: number; period: number; }
export interface ScreenerFilters {
  minTradeValueEok?: number; newHigh?: NMFilter; newHighVol?: NMFilter;
  markets?: ('KOSPI'|'KOSDAQ')[]; excludeEtf?: boolean; q?: string;
}

export function runScreener(f: ScreenerFilters): Promise<ScreenerResponse> {
  const p = new URLSearchParams();
  if (f.minTradeValueEok != null) p.set('min_trade_value_eok', String(f.minTradeValueEok));
  if (f.newHigh) { p.set('nh_lookback', String(f.newHigh.lookback)); p.set('nh_period', String(f.newHigh.period)); }
  if (f.newHighVol) { p.set('nhv_lookback', String(f.newHighVol.lookback)); p.set('nhv_period', String(f.newHighVol.period)); }
  (f.markets ?? []).forEach((m) => p.append('markets', m));
  if (f.excludeEtf) p.set('exclude_etf', 'true');
  if (f.q) p.set('q', f.q);
  return apiCall<ScreenerResponse>(`/api/screener?${p.toString()}`);
}
export const getScreenerStatus = () => apiCall<ScreenerStatus>('/api/screener/status');
export const triggerScreenerUpdate = () => apiCall('/api/screener/update', { method: 'POST' });
```

- [ ] **Step 4: Run to verify it passes** — PASS.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/api/screener.ts frontend/src/api/screener.test.ts
git commit -m "feat(screener-fe): api client + types"
```

---

### Task 12: Hooks

**Files:**
- Create: `frontend/src/screener/useScreener.ts`, `frontend/src/screener/useScreenerStatus.ts`

- [ ] **Step 1: Implement** (react-query 래퍼 — 간단해 단위 테스트 생략, Task 13 통합에서 커버)

```typescript
// frontend/src/screener/useScreener.ts
import { useMutation } from '@tanstack/react-query';
import { runScreener, type ScreenerFilters } from '../api/screener';
export const useScreener = () => useMutation({ mutationFn: (f: ScreenerFilters) => runScreener(f) });

// frontend/src/screener/useScreenerStatus.ts
import { useQuery } from '@tanstack/react-query';
import { getScreenerStatus } from '../api/screener';
export const useScreenerStatus = () =>
  useQuery({ queryKey: ['screener-status'], queryFn: getScreenerStatus, refetchInterval: 30_000 });
```

- [ ] **Step 2: Commit**

```bash
git add frontend/src/screener/useScreener.ts frontend/src/screener/useScreenerStatus.ts
git commit -m "feat(screener-fe): react-query hooks"
```

---

### Task 13: Screener page + components + routing

**Files:**
- Create: `frontend/src/pages/Screener.tsx`, `frontend/src/screener/{ConditionPanel,ResultTable,BreakoutBadge,StalenessChip}.tsx`
- Modify: `frontend/src/main.tsx`, `frontend/src/nav/LeftNav.tsx`
- Test: `frontend/src/pages/Screener.test.tsx`

> **DESIGN.md 정독 필수.** 색·간격·폰트·라운드는 모두 디자인 토큰 사용. 하드코딩 금지.

- [ ] **Step 1: Write failing test** (조회→결과 행 렌더, 클릭→activeCode)

```typescript
// frontend/src/pages/Screener.test.tsx
import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { MemoryRouter } from 'react-router';
import { vi } from 'vitest';
vi.mock('../api/screener', () => ({
  runScreener: vi.fn(async () => ({ status: 'ok', rows: [{
    code: '005930', name: '삼성전자', market: 'KOSPI', price: 317000,
    trade_value_won: 1, change_pct: 5.8,
    new_high: { hit: true, event_date: '20260514', days_ago: 0, period_extreme: 323000 },
    new_high_vol: { hit: false } }] })),
  getScreenerStatus: vi.fn(async () => ({ status: 'ok', last_raw_date: '20260514' })),
}));
const setActiveCode = vi.fn();
vi.mock('../live/useLivePageStore', () => ({ useLivePageStore: (s: any) => s({ setActiveCode }) }));
import { Screener } from './Screener';

it('runs scan and renders row; click sets activeCode', async () => {
  const qc = new QueryClient();
  render(<QueryClientProvider client={qc}><MemoryRouter><Screener /></MemoryRouter></QueryClientProvider>);
  fireEvent.click(screen.getByText('조회'));
  await waitFor(() => screen.getByText('삼성전자'));
  fireEvent.click(screen.getByText('삼성전자'));
  expect(setActiveCode).toHaveBeenCalledWith('005930');
});
```

- [ ] **Step 2: Run to verify it fails** — `cd frontend && npx vitest run src/pages/Screener.test.tsx` → FAIL.

- [ ] **Step 3: Implement** — `Screener.tsx`(조건 패널 + 결과 테이블), `ConditionPanel`(거래대금 하한·신고가 토글+lookback/period·신고거래량·market/ETF), `ResultTable`(행 클릭→`useLivePageStore.setActiveCode(code)`+`navigate('/live')`, ♥→`api/watchlist`, 캡처→`api/captures`), `BreakoutBadge`(`hit` 이면 `N일 전` tooltip), `StalenessChip`(useScreenerStatus). DESIGN.md 토큰 사용. `useScreener` mutation 으로 "조회" 트리거.

- [ ] **Step 4: Run to verify it passes** — PASS. 그리고 `cd frontend && npm run build` 성공.

- [ ] **Step 5: Add routing + nav**

```tsx
// main.tsx: <Route path="screener" element={<Screener />} />
// LeftNav.tsx: <NavItem to="/screener" label="Screener" /> (Workspace 섹션)
```

- [ ] **Step 6: Commit**

```bash
git add frontend/src/pages/Screener.tsx frontend/src/screener/ frontend/src/main.tsx frontend/src/nav/LeftNav.tsx frontend/src/pages/Screener.test.tsx
git commit -m "feat(screener-fe): /screener page (conditions, results, 3 actions)"
```

---

## One-time operational steps (수동, 코드 아님)

구현 완료 후 운영자가 1회 실행:

1. dev-tradingview DB 가동 확인: `docker ps | grep tradingview-db`.
2. 시드: `export_db_to_csv` → `seed_daily_from_csv` → `derive_adjusted` 를 호출하는 CLI/스크립트 1회 실행(`<data_dir>/screener/` 생성, ~10초 + 파생). stocks 도 동일 export.
3. `/screener` 접속 → status `ok` 확인 → "200일 내 500일 신고가" 조회 검증.

---

## Notes / Deferred

- **change_pct 채움**: Task 5 scan 은 `change_pct=None`. base CTE 에 전일 종가 `LAG(close)` 추가해 `(close/prev-1)*100` 산출 → ScreenerRow.change_pct 채우는 것을 Task 5 확장 또는 후속으로(현재 None 허용).
- **KIS 교차검증 폴백(보정 2차)**: `crosscheck_suspect_codes` 는 backlog(spec Risks). 1차 릴리스는 오프라인 A만; 의심 종목 KIS 교체는 후속 PR.
- **period_extreme 신고거래량 단위**: 거래량은 BIGINT 그대로(억원 변환 없음).
- **DuckDB SQL 인젝션**: `q`/`markets` 는 내부 단일사용자 도구라 문자열 보간이나, 실 구현 시 파라미터 바인딩 권장(plan critic 가 지적하면 반영).
