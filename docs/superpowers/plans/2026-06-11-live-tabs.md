# /live 멀티 종목 탭 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `/live`에 멀티 종목 탭을 추가해 사용자가 탭을 열고/닫고/전환하며 여러 종목을 본다.

**Architecture:** cold-swap 뷰어 — 새 `useLiveTabsStore`가 탭 목록을 소유하고, 활성 탭이 기존 `useLivePageStore.{activeCode, candleTimeframe, historicalFromDate}`에 값을 써주는 **단일 writer**다(읽기 15곳 무수정). 백그라운드 warm·디스크 캡처는 기존 Live Set(ADR-0067)이 자동 책임 — 탭은 KIS 구독 한도와 무관. 영속화는 살아남은 `persistentSubscriber.attachPersistence`(`live.tabs.v1`) 재사용. 진입점은 공통 초크포인트 `useJumpToLive` + `LiveSymbolSearch` + `LivePage` URL seed 3곳만 재배선.

**Tech Stack:** React 19, Zustand, TypeScript, Vitest, lightweight-charts. 의존성 `nanoid@5` 이미 설치됨.

**Spec:** `docs/superpowers/specs/2026-06-11-live-tabs-design.md` (v2), `docs/adr/0069-live-multi-tab-reintroduction.md`.

**D2 정정(이 플랜):** 소프트캡 초과 시 "토스트" 대신 **`＋` 버튼 disabled + `8/8` 카운터를 error 색**으로 표시한다(토스트 라이브러리 의존 없이 동일한 "무손실·명시적 피드백" 의도 충족).

---

## File Structure

| 파일 | 책임 | 상태 |
|---|---|---|
| `frontend/src/state/liveTabs.ts` | `useLiveTabsStore` — 탭 CRUD + 활성탭→page 동기화 + 로드/마이그레이션 | Create |
| `frontend/src/state/liveTabs.test.ts` | 스토어 단위 테스트 | Create |
| `frontend/src/live/liveSearchFocus.ts` | `＋`/키보드 → 헤더 검색 포커스 신호 (listener set) | Create |
| `frontend/src/live/LiveTabBar.tsx` | 탭바 UI (DESIGN.md §Tabs) + 상태점 + 닫기 + 미들클릭 + ＋ + 드래그 | Create |
| `frontend/src/live/LiveTabBar.test.tsx` | 탭바 렌더/상호작용 테스트 | Create |
| `frontend/src/live/useJumpToLive.ts` | `setActiveCode` → `openOrFocusTab` 재배선 (관심종목·스크리너·히트맵 공통) | Modify |
| `frontend/src/live/LiveSymbolSearch.tsx` | `setActiveCode` → `openOrFocusTab` (헤더 검색) | Modify |
| `frontend/src/live/LivePage.tsx` | 탭바 행 삽입 + URL seed → `openOrFocusTab` + 마운트 시 활성탭 적용 | Modify |
| `frontend/src/live/useLiveKeyboard.ts` | `[`/`]`·`1`-`9` 탭 전환 콜백 추가 | Modify |

새 클래스/스토어 1개(`useLiveTabsStore`). 신규 파일 5개·수정 4개.

---

## Task 1: Tab 타입 + `useLiveTabsStore` 코어 (CRUD + activeCode 동기화)

옛 `state/tabs.ts`(`git show 7193684~1:frontend/src/state/tabs.ts`)의 `closeTab` 포커스 로직·`nanoid(8)`·소프트캡을 복원하되, `prefs Map`·`bundles`·`cursorMs`는 제거(D1/non-goal). 활성 탭이 `useLivePageStore`에 써주는 단일 writer(D4).

**Files:**
- Create: `frontend/src/state/liveTabs.ts`
- Test: `frontend/src/state/liveTabs.test.ts`

- [ ] **Step 1: 실패 테스트 작성 — openOrFocus / 중복흡수 / closeTab 포커스 / 소프트캡**

```ts
// frontend/src/state/liveTabs.test.ts
import { describe, it, expect, beforeEach } from 'vitest';
import { useLiveTabsStore, TABS_SOFT_CAP } from './liveTabs';
import { useLivePageStore } from './livePage';

beforeEach(() => {
  localStorage.clear();
  useLiveTabsStore.setState({ tabs: [], activeTabId: null });
  useLivePageStore.setState({ activeCode: null, candleTimeframe: '1m', historicalFromDate: null });
});

describe('useLiveTabsStore', () => {
  it('openOrFocusTab creates a tab and focuses it', () => {
    useLiveTabsStore.getState().openOrFocusTab('005930', '삼성전자');
    const { tabs, activeTabId } = useLiveTabsStore.getState();
    expect(tabs).toHaveLength(1);
    expect(tabs[0].code).toBe('005930');
    expect(tabs[0].label).toBe('삼성전자');
    expect(activeTabId).toBe(tabs[0].id);
  });

  it('openOrFocusTab on an existing code focuses, does not duplicate', () => {
    const s = useLiveTabsStore.getState();
    s.openOrFocusTab('005930', '삼성전자');
    s.openOrFocusTab('000660', 'SK하이닉스');
    s.openOrFocusTab('005930', '삼성전자'); // re-open existing
    const { tabs, activeTabId } = useLiveTabsStore.getState();
    expect(tabs).toHaveLength(2);
    expect(tabs.find((t) => t.id === activeTabId)?.code).toBe('005930');
  });

  it('writes the active tab code into useLivePageStore (single writer)', () => {
    useLiveTabsStore.getState().openOrFocusTab('035420', 'NAVER');
    expect(useLivePageStore.getState().activeCode).toBe('035420');
  });

  it('closeTab on active tab focuses the right neighbor (else left)', () => {
    const s = useLiveTabsStore.getState();
    s.openOrFocusTab('A00001');
    s.openOrFocusTab('B00002');
    s.openOrFocusTab('C00003'); // active = C (index 2)
    const mid = useLiveTabsStore.getState().tabs[1].id; // B
    s.focusTab(mid); // active = B (index 1)
    s.closeTab(mid); // closing active middle → right neighbor C
    const { tabs, activeTabId } = useLiveTabsStore.getState();
    expect(tabs.map((t) => t.code)).toEqual(['A00001', 'C00003']);
    expect(tabs.find((t) => t.id === activeTabId)?.code).toBe('C00003');
  });

  it('closing the last tab clears activeTabId and activeCode', () => {
    const s = useLiveTabsStore.getState();
    s.openOrFocusTab('A00001');
    s.closeTab(useLiveTabsStore.getState().tabs[0].id);
    expect(useLiveTabsStore.getState().tabs).toHaveLength(0);
    expect(useLiveTabsStore.getState().activeTabId).toBeNull();
    expect(useLivePageStore.getState().activeCode).toBeNull();
  });

  it('openOrFocusTab is a no-op at the soft cap', () => {
    const s = useLiveTabsStore.getState();
    for (let i = 0; i < TABS_SOFT_CAP; i++) s.openOrFocusTab(`C${String(i).padStart(5, '0')}`);
    s.openOrFocusTab('Z99999'); // over cap
    expect(useLiveTabsStore.getState().tabs).toHaveLength(TABS_SOFT_CAP);
    expect(useLiveTabsStore.getState().tabs.some((t) => t.code === 'Z99999')).toBe(false);
  });

  it('reorderTabs moves a tab', () => {
    const s = useLiveTabsStore.getState();
    s.openOrFocusTab('A00001'); s.openOrFocusTab('B00002'); s.openOrFocusTab('C00003');
    s.reorderTabs(0, 2); // A to the end
    expect(useLiveTabsStore.getState().tabs.map((t) => t.code)).toEqual(['B00002', 'C00003', 'A00001']);
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd frontend && npx vitest run src/state/liveTabs.test.ts`
Expected: FAIL — `Failed to resolve import "./liveTabs"`.

- [ ] **Step 3: 스토어 구현 (동기화·로드 제외, 다음 Task에서 추가)**

```ts
// frontend/src/state/liveTabs.ts
import { create } from 'zustand';
import { nanoid } from 'nanoid';
import { useLivePageStore, type LiveTimeframe } from './livePage';

export const TABS_SOFT_CAP = 8;

export type LiveTab = {
  id: string;
  code: string;
  label: string;
  timeframe: LiveTimeframe;
  historicalFromDate: string | null;
};

type TabsStore = {
  tabs: LiveTab[];
  activeTabId: string | null;
  openOrFocusTab: (code: string, label?: string) => void;
  focusTab: (id: string) => void;
  closeTab: (id: string) => void;
  reorderTabs: (from: number, to: number) => void;
};

// Module guard: when true, the active-tab→page write is in progress, so the
// page→tab mirror (Task 3) must not write back (prevents the historicalFromDate
// reset inside setActiveCode/setCandleTimeframe from clobbering the tab).
export let applyingTab = false;
export function setApplyingTab(v: boolean): void { applyingTab = v; }

/** Push the active tab's view-state into useLivePageStore in the order that
 *  survives the historicalFromDate resets baked into the page setters. */
export function applyTabToPage(tab: LiveTab | null): void {
  setApplyingTab(true);
  const page = useLivePageStore.getState();
  page.setActiveCode(tab?.code ?? null);            // resets historicalFromDate → null
  if (tab) {
    page.setCandleTimeframe(tab.timeframe);          // resets historicalFromDate → null
    if (tab.historicalFromDate) page.extendHistoricalRange(tab.historicalFromDate);
  }
  setApplyingTab(false);
}

export const useLiveTabsStore = create<TabsStore>((set, get) => ({
  tabs: [],
  activeTabId: null,

  openOrFocusTab: (code, label) => {
    const { tabs } = get();
    const hit = tabs.find((t) => t.code === code);
    if (hit) { get().focusTab(hit.id); return; }
    if (tabs.length >= TABS_SOFT_CAP) return; // cap: no-op (UI disables ＋ + reddens counter)
    const tab: LiveTab = {
      id: nanoid(8),
      code,
      label: label ?? code,
      timeframe: useLivePageStore.getState().candleTimeframe, // new tab inherits current tf
      historicalFromDate: null,
    };
    set({ tabs: [...tabs, tab], activeTabId: tab.id });
    applyTabToPage(tab);
  },

  focusTab: (id) => {
    const tab = get().tabs.find((t) => t.id === id);
    if (!tab) return;
    set({ activeTabId: id });
    applyTabToPage(tab);
  },

  closeTab: (id) => {
    const { tabs, activeTabId } = get();
    const idx = tabs.findIndex((t) => t.id === id);
    if (idx === -1) return;
    const next = tabs.filter((t) => t.id !== id);
    if (activeTabId !== id) { set({ tabs: next }); return; }
    // closing the active tab: right neighbor → left → none (옛 closeTab 규칙)
    const nextActiveId = next[idx]?.id ?? next[idx - 1]?.id ?? null;
    set({ tabs: next, activeTabId: nextActiveId });
    applyTabToPage(next.find((t) => t.id === nextActiveId) ?? null);
  },

  reorderTabs: (from, to) => {
    const { tabs } = get();
    if (from < 0 || from >= tabs.length || to < 0 || to >= tabs.length || from === to) return;
    const next = tabs.slice();
    const [moved] = next.splice(from, 1);
    next.splice(to, 0, moved);
    set({ tabs: next });
  },
}));
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd frontend && npx vitest run src/state/liveTabs.test.ts`
Expected: PASS (7 tests).

- [ ] **Step 5: 커밋**

```bash
git add frontend/src/state/liveTabs.ts frontend/src/state/liveTabs.test.ts
git commit -F - <<'EOF'
feat(live): useLiveTabsStore 코어 — 탭 CRUD + activeCode 단일 writer (D4)

옛 state/tabs.ts closeTab 포커스 규칙·nanoid8·소프트캡 복원, prefs Map/bundles/cursor 제거.
EOF
```

> **커밋 훅 주의:** 프로젝트 훅이 `&&`-체이닝/heredoc `git commit`을 오탐 차단할 수 있다. 차단되면 메시지를 파일로 쓰고 단독 `git commit -F <file>`로 우회한다.

---

## Task 2: 영속화 (`live.tabs.v1`) + `live.page.v1` 마이그레이션

살아남은 `persistentSubscriber.attachPersistence`(debounce 250ms) 재사용. 로드는 `live.tabs.v1` → 없으면 `live.page.v1` 1탭 마이그레이션 → 없으면 빈 배열.

**Files:**
- Modify: `frontend/src/state/liveTabs.ts`
- Test: `frontend/src/state/liveTabs.test.ts`

- [ ] **Step 1: 실패 테스트 추가 (마이그레이션 + 라운드트립)**

```ts
// liveTabs.test.ts 에 describe 추가
import { loadTabs, toTabsSnapshot } from './liveTabs';

describe('liveTabs persistence', () => {
  beforeEach(() => localStorage.clear());

  it('migrates live.page.v1 into a single tab when live.tabs.v1 is absent', () => {
    localStorage.setItem('live.page.v1', JSON.stringify({
      activeCode: '005930', candleTimeframe: '5m', historicalFromDate: '20260601',
    }));
    const { tabs, activeTabId } = loadTabs();
    expect(tabs).toHaveLength(1);
    expect(tabs[0].code).toBe('005930');
    expect(tabs[0].timeframe).toBe('5m');
    expect(tabs[0].historicalFromDate).toBe('20260601');
    expect(activeTabId).toBe(tabs[0].id);
  });

  it('loads live.tabs.v1 with activeIndex clamp and reissued ids', () => {
    localStorage.setItem('live.tabs.v1', JSON.stringify({
      version: 1, activeIndex: 9, // out of range → clamps to last
      tabs: [
        { code: '005930', timeframe: '1m', historicalFromDate: null, label: '삼성전자' },
        { code: '000660', timeframe: 'D', historicalFromDate: '20260101', label: 'SK하이닉스' },
      ],
    }));
    const { tabs, activeTabId } = loadTabs();
    expect(tabs.map((t) => t.code)).toEqual(['005930', '000660']);
    expect(tabs[1].timeframe).toBe('D');
    expect(activeTabId).toBe(tabs[1].id); // clamped to last
    expect(tabs[0].id).not.toBe(''); // nanoid reissued
  });

  it('returns empty when neither key exists', () => {
    expect(loadTabs()).toEqual({ tabs: [], activeTabId: null });
  });

  it('toTabsSnapshot drops runtime-only fields', () => {
    const snap = toTabsSnapshot({
      tabs: [{ id: 'abc', code: '005930', label: '삼성전자', timeframe: '1m', historicalFromDate: null }],
      activeTabId: 'abc',
    } as never);
    expect(snap).toEqual({
      version: 1, activeIndex: 0,
      tabs: [{ code: '005930', timeframe: '1m', historicalFromDate: null, label: '삼성전자' }],
    });
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd frontend && npx vitest run src/state/liveTabs.test.ts`
Expected: FAIL — `loadTabs`/`toTabsSnapshot` not exported.

- [ ] **Step 3: 로드·스냅샷·구독 구현 (liveTabs.ts에 추가)**

```ts
// liveTabs.ts — import 추가
import { attachPersistence } from './persistentSubscriber';
import { LIVE_TIMEFRAMES } from './livePage';

const STORAGE_KEY = 'live.tabs.v1';

type TabSnapshot = { code: string; timeframe: LiveTimeframe; historicalFromDate: string | null; label: string };
type TabsSnapshot = { version: 1; activeIndex: number; tabs: TabSnapshot[] };

function isTimeframe(v: unknown): v is LiveTimeframe {
  return typeof v === 'string' && (LIVE_TIMEFRAMES as readonly string[]).includes(v);
}

export function toTabsSnapshot(state: Pick<TabsStore, 'tabs' | 'activeTabId'>): TabsSnapshot {
  const i = state.tabs.findIndex((t) => t.id === state.activeTabId);
  return {
    version: 1,
    activeIndex: i < 0 ? 0 : i,
    tabs: state.tabs.map((t) => ({
      code: t.code, timeframe: t.timeframe, historicalFromDate: t.historicalFromDate, label: t.label,
    })),
  };
}

export function loadTabs(): { tabs: LiveTab[]; activeTabId: string | null } {
  // 1) live.tabs.v1
  try {
    const raw = localStorage.getItem(STORAGE_KEY);
    if (raw) {
      const snap = JSON.parse(raw) as Partial<TabsSnapshot>;
      if (snap && Array.isArray(snap.tabs) && snap.tabs.length > 0) {
        const tabs: LiveTab[] = snap.tabs
          .filter((t) => t && typeof t.code === 'string')
          .map((t) => ({
            id: nanoid(8),
            code: t.code,
            label: typeof t.label === 'string' && t.label ? t.label : t.code,
            timeframe: isTimeframe(t.timeframe) ? t.timeframe : '1m',
            historicalFromDate: typeof t.historicalFromDate === 'string' ? t.historicalFromDate : null,
          }));
        if (tabs.length > 0) {
          const idx = Math.min(Math.max(0, snap.activeIndex ?? 0), tabs.length - 1);
          return { tabs, activeTabId: tabs[idx].id };
        }
      }
    }
  } catch { /* fall through to migration */ }

  // 2) migrate live.page.v1 → single tab
  try {
    const raw = localStorage.getItem('live.page.v1');
    if (raw) {
      const p = JSON.parse(raw) as { activeCode?: string | null; candleTimeframe?: unknown; historicalFromDate?: unknown };
      if (p && typeof p.activeCode === 'string' && p.activeCode) {
        const tab: LiveTab = {
          id: nanoid(8),
          code: p.activeCode,
          label: p.activeCode, // name unknown at migration; tab bar shows code (resolved on next open)
          timeframe: isTimeframe(p.candleTimeframe) ? p.candleTimeframe : '1m',
          historicalFromDate: typeof p.historicalFromDate === 'string' ? p.historicalFromDate : null,
        };
        return { tabs: [tab], activeTabId: tab.id };
      }
    }
  } catch { /* fall through to empty */ }

  // 3) empty
  return { tabs: [], activeTabId: null };
}
```

- [ ] **Step 4: 스토어 초기 상태를 loadTabs로 시드 + 구독 부착**

`useLiveTabsStore` 정의의 `tabs: [], activeTabId: null,` 두 줄을 아래로 교체:

```ts
// const seeded = loadTabs();  // 파일 상단(스토어 create 직전)에 추가
...
export const useLiveTabsStore = create<TabsStore>((set, get) => ({
  ...loadTabs(),   // { tabs, activeTabId }
  // openOrFocusTab/focusTab/closeTab/reorderTabs 동일
}));

// 파일 맨 끝: 영속화 구독 (HMR-safe)
const _unsubscribePersist = attachPersistence(useLiveTabsStore, {
  storageKey: STORAGE_KEY,
  toSnapshot: (s) => toTabsSnapshot(s),
});
if (import.meta.hot) import.meta.hot.dispose(_unsubscribePersist);
```

- [ ] **Step 5: 테스트 통과 확인 + 전체 store 테스트**

Run: `cd frontend && npx vitest run src/state/liveTabs.test.ts`
Expected: PASS (11 tests). `beforeEach`가 `setState({tabs:[], activeTabId:null})`로 리셋하므로 시드 영향 없음.

- [ ] **Step 6: 커밋**

```bash
git add frontend/src/state/liveTabs.ts frontend/src/state/liveTabs.test.ts
git commit -F - <<'EOF'
feat(live): 탭 영속화 live.tabs.v1 + live.page.v1 마이그레이션 (D3)

attachPersistence(debounce 250ms) 재사용. 로드 우선순위 tabs.v1 → page.v1 1탭 → 빈 배열.
EOF
```

---

## Task 3: 활성 탭에 per-tab timeframe·historicalFromDate 미러 (page→tab)

LiveToolbar의 timeframe 변경·차트 팬(historicalFromDate)이 `useLivePageStore`를 바꾼다. 이를 활성 탭에 되써서 per-tab(D1)으로 만든다. `applyingTab` 가드로 focus-push의 reset 클로버를 막는다.

**Files:**
- Modify: `frontend/src/state/liveTabs.ts`
- Test: `frontend/src/state/liveTabs.test.ts`

- [ ] **Step 1: 실패 테스트 추가**

```ts
describe('liveTabs ↔ page mirror', () => {
  beforeEach(() => {
    localStorage.clear();
    useLiveTabsStore.setState({ tabs: [], activeTabId: null });
    useLivePageStore.setState({ activeCode: null, candleTimeframe: '1m', historicalFromDate: null });
  });

  it('toolbar timeframe change writes into the active tab', () => {
    useLiveTabsStore.getState().openOrFocusTab('005930', '삼성전자');
    useLivePageStore.getState().setCandleTimeframe('5m'); // user toolbar action
    const active = useLiveTabsStore.getState().tabs[0];
    expect(active.timeframe).toBe('5m');
  });

  it('switching tabs restores each tab timeframe', () => {
    const s = useLiveTabsStore.getState();
    s.openOrFocusTab('005930', '삼성전자');
    useLivePageStore.getState().setCandleTimeframe('5m'); // tab A → 5m
    s.openOrFocusTab('000660', 'SK하이닉스');             // tab B (inherits 5m)
    useLivePageStore.getState().setCandleTimeframe('D');  // tab B → D
    const tabA = useLiveTabsStore.getState().tabs[0].id;
    s.focusTab(tabA);
    expect(useLivePageStore.getState().candleTimeframe).toBe('5m'); // A restored
  });

  it('pan (historicalFromDate) persists per tab', () => {
    useLiveTabsStore.getState().openOrFocusTab('005930', '삼성전자');
    useLivePageStore.getState().extendHistoricalRange('20260601');
    expect(useLiveTabsStore.getState().tabs[0].historicalFromDate).toBe('20260601');
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd frontend && npx vitest run src/state/liveTabs.test.ts -t mirror`
Expected: FAIL — timeframe stays '1m' (no mirror yet).

- [ ] **Step 3: 미러 구독 구현 (liveTabs.ts 맨 끝, attachPersistence 위에 추가)**

```ts
// page→tab mirror: user-initiated tf/pan changes flow into the active tab.
// Guarded by applyingTab so the focus-push's setActiveCode/setCandleTimeframe
// resets don't write a transient null back into the tab.
const _unsubscribeMirror = useLivePageStore.subscribe((state, prev) => {
  if (applyingTab) return;
  if (state.candleTimeframe === prev.candleTimeframe && state.historicalFromDate === prev.historicalFromDate) return;
  const { tabs, activeTabId } = useLiveTabsStore.getState();
  if (!activeTabId) return;
  useLiveTabsStore.setState({
    tabs: tabs.map((t) =>
      t.id === activeTabId
        ? { ...t, timeframe: state.candleTimeframe, historicalFromDate: state.historicalFromDate }
        : t,
    ),
  });
});
if (import.meta.hot) import.meta.hot.dispose(_unsubscribeMirror);
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd frontend && npx vitest run src/state/liveTabs.test.ts`
Expected: PASS (14 tests).

- [ ] **Step 5: 커밋**

```bash
git add frontend/src/state/liveTabs.ts frontend/src/state/liveTabs.test.ts
git commit -F - <<'EOF'
feat(live): per-tab timeframe·pan 미러 (D1) — applyingTab 가드로 reset 클로버 방지
EOF
```

---

## Task 4: 진입점 재배선 (`useJumpToLive` · `LiveSymbolSearch`)

4개 패널(관심종목·스크리너·스크리너페이지·히트맵)은 `useJumpToLive` 하나를 통하므로 그 훅만 고치면 전부 커버. 헤더 검색은 `LiveSymbolSearch` 직접.

**Files:**
- Modify: `frontend/src/live/useJumpToLive.ts`
- Modify: `frontend/src/live/LiveSymbolSearch.tsx`
- Test: `frontend/src/live/useJumpToLive.test.tsx` (Create)

- [ ] **Step 1: 실패 테스트 작성**

```tsx
// frontend/src/live/useJumpToLive.test.tsx
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { renderHook } from '@testing-library/react';
import { MemoryRouter } from 'react-router';
import { useJumpToLive } from './useJumpToLive';
import { useLiveTabsStore } from '../state/liveTabs';
import { useLivePageStore } from '../state/livePage';

beforeEach(() => {
  localStorage.clear();
  useLiveTabsStore.setState({ tabs: [], activeTabId: null });
  useLivePageStore.setState({ activeCode: null, candleTimeframe: '1m', historicalFromDate: null });
});

it('jump opens-or-focuses a tab instead of only setting activeCode', () => {
  const { result } = renderHook(() => useJumpToLive(), {
    wrapper: ({ children }) => <MemoryRouter initialEntries={['/live']}>{children}</MemoryRouter>,
  });
  result.current('005930');
  expect(useLiveTabsStore.getState().tabs.map((t) => t.code)).toEqual(['005930']);
  expect(useLivePageStore.getState().activeCode).toBe('005930');
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd frontend && npx vitest run src/live/useJumpToLive.test.tsx`
Expected: FAIL — tabs stays empty (still calls setActiveCode only).

- [ ] **Step 3: `useJumpToLive` 재배선**

```ts
// frontend/src/live/useJumpToLive.ts
import { useNavigate, useLocation } from 'react-router';
import { useLiveTabsStore } from '../state/liveTabs';

/** 차트로 점프: 종목 탭을 열거나 포커스하고, /live 가 아니면 이동한다.
 *  관심종목/스크리너/히트맵 행 클릭의 공통 jump-to-chart 동작(CONTEXT.md).
 *  탭 도입(ADR-0069) 이후 setActiveCode 직접 호출 대신 openOrFocusTab을 쓴다 —
 *  활성 탭이 useLivePageStore.activeCode의 단일 writer(D4). */
export function useJumpToLive() {
  const openOrFocusTab = useLiveTabsStore((s) => s.openOrFocusTab);
  const navigate = useNavigate();
  const { pathname } = useLocation();
  return (code: string, label?: string) => {
    openOrFocusTab(code, label);
    if (pathname !== '/live') navigate('/live');
  };
}
```

> 호출부(`WatchlistDrawer`/`ScreenerDrawer`/`Screener`/`Heatmap`)는 `onPick(code)` 그대로 동작한다. 종목명을 넘기면(`onPick(code, name)`) 탭 라벨이 채워지지만, 시그니처가 optional이라 무수정으로도 컴파일·동작한다(라벨=코드 폴백).

- [ ] **Step 4: `LiveSymbolSearch` 재배선**

`frontend/src/live/LiveSymbolSearch.tsx`:
- L4 `import { useLivePageStore } from '../state/livePage';` → `import { useLiveTabsStore } from '../state/liveTabs';`
- L11 `const setActiveCode = useLivePageStore((s) => s.setActiveCode);` → `const openOrFocusTab = useLiveTabsStore((s) => s.openOrFocusTab);`
- L21 `const selectHit = (hit: SymbolHit) => { setActiveCode(hit.code); setQuery(''); };` → `const selectHit = (hit: SymbolHit) => { openOrFocusTab(hit.code, hit.name); setQuery(''); };`
- L30 `if (/^\d{6}$/.test(t)) { setActiveCode(t); setQuery(''); return true; }` → `if (/^\d{6}$/.test(t)) { openOrFocusTab(t); setQuery(''); return true; }`

- [ ] **Step 5: 테스트 통과 + 타입체크**

Run: `cd frontend && npx vitest run src/live/useJumpToLive.test.tsx && npx tsc -p tsconfig.app.json --noEmit`
Expected: PASS + 타입 에러 0건 (권위 타입체크는 `tsconfig.app.json`).

- [ ] **Step 6: 커밋**

```bash
git add frontend/src/live/useJumpToLive.ts frontend/src/live/LiveSymbolSearch.tsx frontend/src/live/useJumpToLive.test.tsx
git commit -F - <<'EOF'
feat(live): 진입점 재배선 — 검색/관심종목/스크리너/히트맵 → openOrFocusTab (D5)
EOF
```

---

## Task 5: 검색 포커스 신호 + `LiveTabBar` 컴포넌트 (UI)

DESIGN.md §Tabs + 승인 목업 그대로. `＋`는 `liveSearchFocus`로 헤더 검색에 포커스. 닫기 = `×` + 미들클릭. 캡 시 `＋` disabled + 카운터 error색(D2 정정). 드래그는 Task 8.

**Files:**
- Create: `frontend/src/live/liveSearchFocus.ts`
- Create: `frontend/src/live/LiveTabBar.tsx`
- Modify: `frontend/src/live/LiveSymbolSearch.tsx` (포커스 리스너 등록)
- Test: `frontend/src/live/LiveTabBar.test.tsx`

- [ ] **Step 1: 검색 포커스 신호 모듈 (테스트 불요 — 자명한 listener set)**

```ts
// frontend/src/live/liveSearchFocus.ts
type Listener = () => void;
const listeners = new Set<Listener>();

/** LiveSymbolSearch가 mount 시 자신의 input focus 함수를 등록한다. */
export function onFocusLiveSearch(fn: Listener): () => void {
  listeners.add(fn);
  return () => listeners.delete(fn);
}

/** ＋ 버튼·키보드가 헤더 검색에 포커스를 요청한다. */
export function focusLiveSearch(): void {
  listeners.forEach((fn) => fn());
}
```

- [ ] **Step 2: `LiveSymbolSearch`에 포커스 리스너 등록**

`LiveSymbolSearch.tsx`의 "/" 핸들러 useEffect 아래에 추가:

```tsx
import { onFocusLiveSearch } from './liveSearchFocus';
// ...
useEffect(() => onFocusLiveSearch(() => inputRef.current?.focus()), [inputRef]);
```

- [ ] **Step 3: 실패 테스트 작성 (탭바 렌더/포커스/닫기/캡)**

```tsx
// frontend/src/live/LiveTabBar.test.tsx
import { describe, it, expect, vi } from 'vitest';
import { type ComponentProps } from 'react';
import { render, screen, fireEvent } from '@testing-library/react';
import { LiveTabBar } from './LiveTabBar';
import type { LiveTab } from '../state/liveTabs';

const tabs: LiveTab[] = [
  { id: 'a', code: '005930', label: '삼성전자', timeframe: '1m', historicalFromDate: null },
  { id: 'b', code: '000660', label: 'SK하이닉스', timeframe: '1m', historicalFromDate: null },
];

function setup(over: Partial<ComponentProps<typeof LiveTabBar>> = {}) {
  const props: ComponentProps<typeof LiveTabBar> = {
    tabs, activeTabId: 'a', activeLoading: false, atCap: false,
    onFocus: vi.fn(), onClose: vi.fn(), onReorder: vi.fn(), onNewTab: vi.fn(),
    ...over,
  };
  render(<LiveTabBar {...props} />);
  return props;
}

it('renders the stock name only (code hidden when the name is known)', () => {
  setup();
  expect(screen.getByText('삼성전자')).toBeInTheDocument();
  expect(screen.queryByText('005930')).toBeNull(); // code not shown as text (aria-label only)
});

it('falls back to the code when the name is unknown (label === code)', () => {
  setup({
    tabs: [{ id: 'x', code: '123456', label: '123456', timeframe: '1m', historicalFromDate: null }],
    activeTabId: 'x',
  });
  expect(screen.getByText('123456')).toBeInTheDocument();
});

it('clicking a tab calls onFocus with its id', () => {
  const p = setup();
  fireEvent.click(screen.getByText('SK하이닉스'));
  expect(p.onFocus).toHaveBeenCalledWith('b');
});

it('clicking the close button calls onClose, not onFocus', () => {
  const p = setup();
  fireEvent.click(screen.getByLabelText('005930 닫기'));
  expect(p.onClose).toHaveBeenCalledWith('a');
});

it('middle-click closes the tab', () => {
  const p = setup();
  fireEvent.mouseDown(screen.getByText('SK하이닉스').closest('[data-tab-id]')!, { button: 1 });
  expect(p.onClose).toHaveBeenCalledWith('b');
});

it('the new-tab button calls onNewTab', () => {
  const p = setup();
  fireEvent.click(screen.getByLabelText('새 탭'));
  expect(p.onNewTab).toHaveBeenCalled();
});

it('disables the new-tab button at the cap', () => {
  setup({ atCap: true });
  expect(screen.getByLabelText('새 탭')).toBeDisabled();
});
```

- [ ] **Step 4: 테스트 실패 확인**

Run: `cd frontend && npx vitest run src/live/LiveTabBar.test.tsx`
Expected: FAIL — cannot resolve `./LiveTabBar`.

- [ ] **Step 5: `LiveTabBar` 구현 (DESIGN.md §Tabs 토큰)**

```tsx
// frontend/src/live/LiveTabBar.tsx
import { type CSSProperties } from 'react';
import { TABS_SOFT_CAP, type LiveTab } from '../state/liveTabs';

interface Props {
  tabs: LiveTab[];
  activeTabId: string | null;
  /** 활성 탭의 차트 데이터 로딩 중 여부 (상태점). */
  activeLoading: boolean;
  atCap: boolean;
  onFocus: (id: string) => void;
  onClose: (id: string) => void;
  onReorder: (from: number, to: number) => void; // Task 8에서 드래그 핸들러가 사용
  onNewTab: () => void;
}

/** 상태점: 활성+로딩=accent pulse(◌), 활성+로드=success(●), 비활성=dimmer outline(○). */
function statusDotStyle(active: boolean, loading: boolean): CSSProperties {
  if (active && loading) return { background: 'var(--accent)', animation: 'tab-pulse 1.5s ease-in-out infinite' };
  if (active) return { background: 'var(--success)', boxShadow: '0 0 4px rgba(34,197,94,0.5)' };
  return { background: 'transparent', border: '1px solid var(--fg-dimmer)' };
}

export function LiveTabBar({ tabs, activeTabId, activeLoading, atCap, onFocus, onClose, onNewTab }: Props) {
  return (
    <div className="flex items-end gap-0.5 h-full px-2 font-ui" style={{ background: 'var(--bg-subtle)' }}>
      {tabs.map((t) => {
        const active = t.id === activeTabId;
        return (
          <div
            key={t.id}
            data-tab-id={t.id}
            role="tab"
            aria-selected={active}
            onClick={() => onFocus(t.id)}
            onMouseDown={(e) => { if (e.button === 1) { e.preventDefault(); onClose(t.id); } }}
            className="relative flex items-center gap-1.5 h-8 px-2.5 rounded-t-md cursor-pointer select-none group"
            style={{
              background: active ? 'var(--bg-card)' : 'var(--bg-input)',
              border: active ? 'none' : '1px solid var(--border)',
              borderBottom: 'none',
            }}
          >
            {active && (
              <span className="absolute left-0 right-0 top-0 h-[2px]" style={{ background: 'var(--accent)' }} />
            )}
            <span className="w-1.5 h-1.5 rounded-full shrink-0" style={statusDotStyle(active, activeLoading)} />
            {/* 종목명만 표시 (확정 2026-06-11). label은 종목명이며, 이름을 모르는 탭(마이그레이션 등)만
                label===code 폴백으로 코드가 보인다. 긴 이름은 말줄임. */}
            <span className="text-sm shrink-0 max-w-[160px] truncate" title={t.label} style={{ color: active ? 'var(--fg)' : 'var(--fg-dim)' }}>{t.label}</span>
            <button
              type="button"
              aria-label={`${t.code} 닫기`}
              onClick={(e) => { e.stopPropagation(); onClose(t.id); }}
              className="ml-1 w-[18px] h-[18px] flex items-center justify-center rounded opacity-0 group-hover:opacity-100"
              style={{ color: 'var(--fg-dimmer)' }}
            >
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.2" className="w-[11px] h-[11px]">
                <line x1="18" y1="6" x2="6" y2="18" /><line x1="6" y1="6" x2="18" y2="18" />
              </svg>
            </button>
          </div>
        );
      })}
      <button
        type="button"
        aria-label="새 탭"
        onClick={onNewTab}
        disabled={atCap}
        className="ml-1 w-7 h-7 flex items-center justify-center rounded-md disabled:opacity-40 disabled:cursor-not-allowed"
        style={{ color: 'var(--fg-dim)', border: '1px solid var(--border)' }}
      >
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.4" className="w-[13px] h-[13px]">
          <line x1="12" y1="5" x2="12" y2="19" /><line x1="5" y1="12" x2="19" y2="12" />
        </svg>
      </button>
      <span className="ml-auto self-center text-xs font-mono" style={{ color: atCap ? 'var(--error)' : 'var(--fg-dimmer)' }}>
        {tabs.length}/{TABS_SOFT_CAP} open
      </span>
    </div>
  );
}
```

`tab-pulse` keyframe을 `frontend/src/styles/tokens.css` 끝에 추가(없으면):

```css
@keyframes tab-pulse { 0%, 100% { opacity: 1; } 50% { opacity: 0.3; } }
```

- [ ] **Step 6: 테스트 통과 확인**

Run: `cd frontend && npx vitest run src/live/LiveTabBar.test.tsx`
Expected: PASS (6 tests).

- [ ] **Step 7: 커밋**

```bash
git add frontend/src/live/LiveTabBar.tsx frontend/src/live/LiveTabBar.test.tsx frontend/src/live/liveSearchFocus.ts frontend/src/live/LiveSymbolSearch.tsx frontend/src/styles/tokens.css
git commit -F - <<'EOF'
feat(live): LiveTabBar UI (DESIGN.md §Tabs) + 검색 포커스 신호 + 미들클릭 닫기
EOF
```

---

## Task 6: 그리드 통합 (`LivePage`) + 마운트 시 활성 탭 적용

탭바 행을 헤더와 상태바 사이에 삽입. URL seed → `openOrFocusTab`. 마운트 시 복원된 활성 탭을 page에 적용. 탭이 0개면 기존 `LiveEmptyState`(activeCode=null 경로) 그대로.

**Files:**
- Modify: `frontend/src/live/LivePage.tsx`
- Test: 수동 브라우저 검증 (`/browse`) — 통합 동작.

- [ ] **Step 1: `LivePage` 수정 — import + 스토어 구독**

`LivePage.tsx` import에 추가:

```tsx
import { LiveTabBar } from './LiveTabBar';
import { useLiveTabsStore, TABS_SOFT_CAP } from '../state/liveTabs';
```

- [ ] **Step 2: URL seed + 마운트 적용 effect 교체**

기존 seed effect(L49-54)를 교체:

```tsx
const tabs = useLiveTabsStore((s) => s.tabs);
const activeTabId = useLiveTabsStore((s) => s.activeTabId);
const openOrFocusTab = useLiveTabsStore((s) => s.openOrFocusTab);
const focusTab = useLiveTabsStore((s) => s.focusTab);
const closeTab = useLiveTabsStore((s) => s.closeTab);
const reorderTabs = useLiveTabsStore((s) => s.reorderTabs);

// 1회: URL ?code= 시드는 복원된 탭 위에 open-or-focus, 없으면 복원된 활성 탭을 page에 적용.
const seeded = useRef(false);
useEffect(() => {
  if (seeded.current) return;
  seeded.current = true;
  if (queryCode) openOrFocusTab(queryCode);
  else if (activeTabId) focusTab(activeTabId); // 복원된 활성 탭 → page 동기화
}, [queryCode, activeTabId, openOrFocusTab, focusTab]);
```

> `storedCode`/`setActiveCode` 참조와 기존 seed effect는 제거한다. `activeCode = storedCode`(L64)는 `const activeCode = useLivePageStore((s) => s.activeCode);`로 유지(읽기 무수정).

- [ ] **Step 3: 탭바 행을 그리드에 삽입**

`gridTemplateRows`에 탭 행 추가(헤더 다음):

```tsx
gridTemplateRows:
  'var(--h-live-header) 40px auto var(--h-pricestrip) var(--h-toolbar) minmax(0, 1fr)',
```

`<LiveHeader />` 바로 아래에 삽입:

```tsx
<LiveTabBar
  tabs={tabs}
  activeTabId={activeTabId}
  activeLoading={isPastCandlesLoading}
  atCap={tabs.length >= TABS_SOFT_CAP}
  onFocus={focusTab}
  onClose={closeTab}
  onReorder={reorderTabs}
  onNewTab={focusLiveSearch}
/>
```

`import { focusLiveSearch } from './liveSearchFocus';` 추가.

- [ ] **Step 4: 빌드 + 타입체크**

Run: `cd frontend && npx tsc -p tsconfig.app.json --noEmit && npm run build`
Expected: 타입 0건 + build 그린.

- [ ] **Step 5: 브라우저 통합 검증**

```bash
# 백엔드(:8000)·프론트(:5173) 데브서버 기동 후
B=/home/dev/.claude/skills/gstack/browse/dist/browse
$B goto http://localhost:5173/live
$B console --errors        # 에러 0
```
체크: 검색으로 종목 2개 열기 → 탭 2개 생성, 활성 탭만 차트 렌더. 탭 클릭 전환. `×`로 닫기 → 인접 탭 포커스. 마지막 탭 닫으면 `/` 안내 빈 상태. 새로고침 → 탭 복원.

- [ ] **Step 6: 커밋**

```bash
git add frontend/src/live/LivePage.tsx
git commit -F - <<'EOF'
feat(live): LivePage에 탭바 행 통합 + URL seed/복원 활성탭 적용 (D7/D8)
EOF
```

---

## Task 7: 키보드 탭 전환 (`[`/`]`·`1`-`9`)

기존 `useLiveKeyboard` 비수정자 패턴 확장(Ctrl 금지 — 브라우저 충돌, D6). `[`=이전, `]`=다음, `1`-`9`=N번 탭.

**Files:**
- Modify: `frontend/src/live/useLiveKeyboard.ts`
- Modify: `frontend/src/live/LivePage.tsx`
- Test: `frontend/src/live/useLiveKeyboard.test.ts` (Create)

- [ ] **Step 1: 실패 테스트 작성**

```ts
// frontend/src/live/useLiveKeyboard.test.ts
import { describe, it, expect, vi } from 'vitest';
import { renderHook } from '@testing-library/react';
import { useLiveKeyboard } from './useLiveKeyboard';

function press(key: string, init: KeyboardEventInit = {}) {
  window.dispatchEvent(new KeyboardEvent('keydown', { key, ...init }));
}

it('] calls onNextTab, [ calls onPrevTab', () => {
  const onNextTab = vi.fn(), onPrevTab = vi.fn();
  renderHook(() => useLiveKeyboard({ onNextTab, onPrevTab }));
  press(']'); expect(onNextTab).toHaveBeenCalled();
  press('['); expect(onPrevTab).toHaveBeenCalled();
});

it('digit keys call onSelectTabIndex with 0-based index', () => {
  const onSelectTabIndex = vi.fn();
  renderHook(() => useLiveKeyboard({ onSelectTabIndex }));
  press('3'); expect(onSelectTabIndex).toHaveBeenCalledWith(2);
});

it('ignores Ctrl-modified keys (browser-reserved)', () => {
  const onNextTab = vi.fn();
  renderHook(() => useLiveKeyboard({ onNextTab }));
  press(']', { ctrlKey: true }); expect(onNextTab).not.toHaveBeenCalled();
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd frontend && npx vitest run src/live/useLiveKeyboard.test.ts`
Expected: FAIL — opts에 onNextTab/onPrevTab/onSelectTabIndex 없음.

- [ ] **Step 3: `useLiveKeyboard` 확장**

`UseLiveKeyboardOpts`에 추가:

```ts
export interface UseLiveKeyboardOpts {
  onNextCode?: () => void;
  onPrevCode?: () => void;
  onNextTab?: () => void;
  onPrevTab?: () => void;
  onSelectTabIndex?: (index: number) => void;
}
```

`switch (e.key)`에 케이스 추가(`'k'` 케이스 다음):

```ts
case ']':
  opts.onNextTab?.();
  e.preventDefault();
  break;
case '[':
  opts.onPrevTab?.();
  e.preventDefault();
  break;
default:
  if (e.key >= '1' && e.key <= '9' && opts.onSelectTabIndex) {
    opts.onSelectTabIndex(Number(e.key) - 1);
    e.preventDefault();
  }
  break;
```

`useEffect` 의존성 배열에 새 콜백 추가: `[opts.onNextCode, opts.onPrevCode, opts.onNextTab, opts.onPrevTab, opts.onSelectTabIndex]`.

- [ ] **Step 4: `LivePage`에서 콜백 wiring**

`LivePage.tsx`의 `useLiveKeyboard({})`를 교체:

```tsx
const activeIdx = tabs.findIndex((t) => t.id === activeTabId);
useLiveKeyboard({
  onNextTab: () => { if (tabs.length) focusTab(tabs[(activeIdx + 1 + tabs.length) % tabs.length].id); },
  onPrevTab: () => { if (tabs.length) focusTab(tabs[(activeIdx - 1 + tabs.length) % tabs.length].id); },
  onSelectTabIndex: (i) => { if (i < tabs.length) focusTab(tabs[i].id); },
});
```

- [ ] **Step 5: 테스트 통과 + 타입체크**

Run: `cd frontend && npx vitest run src/live/useLiveKeyboard.test.ts && npx tsc -p tsconfig.app.json --noEmit`
Expected: PASS (3 tests) + 타입 0건.

- [ ] **Step 6: 커밋**

```bash
git add frontend/src/live/useLiveKeyboard.ts frontend/src/live/useLiveKeyboard.test.ts frontend/src/live/LivePage.tsx
git commit -F - <<'EOF'
feat(live): 비수정자 키보드 탭 전환 [/]·1-9 (D6, Ctrl 금지=브라우저 충돌)
EOF
```

---

## Task 8: 드래그 재정렬 (네이티브 HTML5 draggable)

라이브러리 없이 표준 draggable로 탭 순서 변경 → `reorderTabs`. 옛 watchlist dnd 의존 없음(Layer-1 자족).

**Files:**
- Modify: `frontend/src/live/LiveTabBar.tsx`
- Test: `frontend/src/live/LiveTabBar.test.tsx`

- [ ] **Step 1: 실패 테스트 추가**

```tsx
it('drag-and-drop reorders via onReorder(from, to)', () => {
  const p = setup();
  const elA = screen.getByText('삼성전자').closest('[data-tab-id]')!;
  const elB = screen.getByText('SK하이닉스').closest('[data-tab-id]')!;
  fireEvent.dragStart(elA);                 // from index 0
  fireEvent.dragOver(elB);
  fireEvent.drop(elB);                       // to index 1
  expect(p.onReorder).toHaveBeenCalledWith(0, 1);
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd frontend && npx vitest run src/live/LiveTabBar.test.tsx -t reorder`
Expected: FAIL — onReorder not called.

- [ ] **Step 3: 드래그 핸들러 추가 (LiveTabBar.tsx)**

탭 `map`의 인덱스를 받도록 `tabs.map((t, idx) => ...)`로 바꾸고, 탭 `<div>`에 추가:

```tsx
draggable
onDragStart={(e) => { e.dataTransfer.setData('text/tab-index', String(idx)); e.dataTransfer.effectAllowed = 'move'; }}
onDragOver={(e) => e.preventDefault()}
onDrop={(e) => {
  e.preventDefault();
  const from = Number(e.dataTransfer.getData('text/tab-index'));
  if (Number.isInteger(from) && from !== idx) onReorder(from, idx);
}}
```

`Props` 구조분해에 `onReorder` 포함(이미 인터페이스에 있음 — 사용처 추가).

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd frontend && npx vitest run src/live/LiveTabBar.test.tsx`
Expected: PASS (7 tests).

- [ ] **Step 5: 커밋**

```bash
git add frontend/src/live/LiveTabBar.tsx frontend/src/live/LiveTabBar.test.tsx
git commit -F - <<'EOF'
feat(live): 탭 드래그 재정렬 (네이티브 HTML5 draggable → reorderTabs)
EOF
```

---

## Final: 전체 검증

- [ ] **타입체크 + 빌드 + 전체 테스트**

```bash
cd frontend
npx tsc -p tsconfig.app.json --noEmit   # 권위 타입체크 (root tsconfig는 인자없이 무동작)
npm run build
npx vitest run src/state/liveTabs.test.ts src/live/LiveTabBar.test.tsx src/live/useJumpToLive.test.tsx src/live/useLiveKeyboard.test.ts
```
Expected: 타입 0건, build 그린, 전 테스트 PASS.

- [ ] **브라우저 회귀 (장중 권장)**: 검색→탭2개 / 전환 / `×`·미들클릭 닫기 / `[`·`]`·`1`-`9` / 드래그 재정렬 / 새로고침 복원 / 관심종목·히트맵 클릭→탭 열림 / 타임프레임 탭별 유지.

---

## Self-Review (spec 대조)

- **D1 timeframe 탭별 / prefs 전역**: Task 3 미러로 timeframe·pan 탭별. ChartViewPrefs는 미터치(전역 유지) ✓
- **D2 소프트캡**: Task 1 no-op + Task 5 `＋` disabled·카운터 error색(토스트 정정 명시) ✓
- **D3 영속화**: Task 2 `attachPersistence` `live.tabs.v1` + page.v1 마이그레이션 ✓
- **D4 activeCode 유지+단일writer**: `applyTabToPage`가 `setActiveCode` 호출, 읽기 15곳 무수정 ✓
- **D5 ＋=검색 재사용**: Task 5 `liveSearchFocus` (새 팝오버 없음) ✓
- **D6 키보드 비수정자**: Task 7 `[`/`]`·1-9, Ctrl 무시 테스트 ✓
- **D7 마지막탭 빈상태**: closeTab→activeCode null→기존 `LiveEmptyState` ✓
- **D8 URL seed**: Task 6 복원 후 `?code=` open-or-focus ✓
- **진입점 4곳**: `useJumpToLive`(관심종목·스크리너·스크리너페이지·히트맵) + `LiveSymbolSearch` + `LivePage` ✓

## 이 플랜 밖 (fast-follow, 별도 플랜)

- **수동 핀(Live Set 편입)** — ADR-0069 "later". Live Set lifecycle(`refresh_live_stream`) 연동 필요, 백엔드 작업 동반.
- **탭별 ChartViewPrefs** — 삭제한 `Map<tabId>` 인디렉션 부활. reversible, 수요 확인 후.
- **상태점 정밀 매핑** — 현재 active loaded/loading만. 전체 수집등급(`deriveCollectionStatus`: 실시간/준실시간/파킹) 매핑은 후속.
- **CONTEXT.md 글로서리** — 구현 랜딩 시 `activeCode` 엔트리 갱신 + "Live Tab" 용어 추가(ADR-0069 Consequences).
- **`liveTabs.ts` 모듈-로드 부작용 정리** (Task 4 리뷰 발견) — import 시 `attachPersistence`+`useLivePageStore.subscribe`가 실행돼 무관 테스트에 두 번째로 파급(Task 2·Task 4). 권장: (a) subscribe/persistence 와이어링을 `initLiveTabsSync()`로 빼 앱 엔트리에서 1회 호출(passive-import 부작용 제거; SSR/무관 테스트 위생). 단 **(a)만으론 부족** — `openOrFocusTab`/`applyTabToPage`가 매 클릭 `useLivePageStore.getState()`를 호출하므로 selector-only mock 테스트는 여전히 깨짐. (b) 소비자 테스트가 `liveTabs` 대신 `useJumpToLive`를 mock하도록 표준화 = 파급 근본 종결. 단일 SPA라 긴급도 낮음.
