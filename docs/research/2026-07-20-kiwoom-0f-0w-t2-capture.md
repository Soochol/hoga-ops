# 키움 0F·0w 정규장 실채록 (T2) — PR-F 언블록 근거

- 일시: 2026-07-20 (월) 10:27~10:32 KST, 정규장.
- 접속: account 2(=`KIWOOM_APP_KEY_3`, 유휴 키 — 운영 conn 계정 0·1과 상호 킥 없음),
  `wss://api.kiwoom.com:10000/api/dostk/websocket`, LOGIN rc=0, REG rc=0.
- 구독: `["005930","000660","373220","207940","005380"]` × types `["0B","0F","0w"]`
  (0D는 레이아웃 기지·틱 폭주라 제외).
- 수신 5분: **0B 6,880 · 0w 205 · 0F 24** — 5종목 전부에서 0F(각 4~5회)·0w(각 38~43회)
  push 유입 확인. **스모크 정방향 — PR-F 언블록.**
- 원본: `~/.local/share/hoga-ops/data/research_capture/20260720/t2_frames_0B0F0w_account2.jsonl`
  (5.6MB, 프레임별 `recv_ms` 포함). 대표 13프레임:
  `tests/fixtures/kiwoom_t2/golden_frames_20260720.json`.

## 0F 주식당일거래원 — FID 57개 (5쌍 구조)

| 의미 | 매도 1~5위 | 매수 1~5위 |
|---|---|---|
| 거래원 이름 | 141–145 | 151–155 |
| 수량(누적) | 161–165 | 171–175 |
| 수량 증감(부호) | 166–170 | 176–180 |
| 회원사 코드(3자리) | 146–150 | 156–160 |
| 미확정(외국계 관련) | 271–275 | 281–285 |

- ADR-0111 BROKER 합성 틱(buy_top/sell_top 상위5 회원사명+수량)과 정확히 대응 —
  파서는 141–180(+146–160 코드)만 소비하면 shape-compat.
- 271–275/281–285: 국내 거래원은 `"!!!!"`, 외국계(맥쿼리 035·JP모간 033·UBS 043·
  메릴린치 044)만 `DGDB`/`DHDB` 류 값. 의미 미확정 — 파서 미소비 권장.
- 261/262 (외국계 매도 합/증감 추정), 263/264 (매수 합/증감 추정), 267/268
  (순매수/증감 추정), 337 (갱신 구분 추정) — 시계열 대조로 확정 가능(원본 24건).

## 0w 종목별프로그램매매 — FID 14개

- `20` 시간 HHMMSS · `10` 현재가(부호=등락방향) · `11` 전일대비 · `12` 등락율 ·
  `13` 누적거래량 · `25` 전일대비기호(추정).
- `202/204/206/208/210/212`: 프로그램 매도/매수 수급 계열(금액·수량 쌍 추정),
  `211/213`: 미확정. **program_trade_store 매핑은 원본 205건 시계열 대조로 확정할 것**
  (예: 202-204=순매수 검산, KIS 프로그램 REST 응답과 동시각 대조).

## 0B 규약 확정 (kiwoom_fields.py NOTE 해소)

- **FID 15 부호 = 체결방향 확정**: 정규장 5분간 +4,295 / −2,585, 무부호 0건
  (0은 동시호가 전용).
- **FID 20 체결시각 = 이벤트 실시각**: 수신 벽시계 대비 0.04~1.09s(p50 0.54s) 지연 —
  시프트·미래 라벨 없음.
- 정규장 0B FID 43개 관측(애프터마켓 대비 확장: 1030~1892 대역 추가) — 기존 파서
  소비 필드(10/15/20)는 동일.

## cntr_tm(ka10080 REST 분봉) 라벨 확정 — Minor 해소

10:35:19 호출 → 최신 캔들 라벨 `10:35:00`(진행 중 분을 그 분의 **시작**으로 표기).
**시작 라벨 확정 — 키움 날짜 1분 시프트 없음.** 우려했던 끝 라벨 아님.

## 후속 (PR-F 실행 세션)

1. `kiwoom_fields.py`에 0F/0w FID 상수 추가(위 표 + 원본 대조).
2. `kiwoom_frames`에 0F→BROKER(`_parse_member` shape-compat)·0w→program 파서,
   골든테스트 = `tests/fixtures/kiwoom_t2/golden_frames_20260720.json`.
3. 저장셋 구독 types에 0F/0w 추가(슬롯 불변 — 종목 단위 실측 2026-07-16).
4. BrokerRestPoller·프로그램 REST 폴러 정지·삭제(KIS 유량 캔들·지수로 환원).
