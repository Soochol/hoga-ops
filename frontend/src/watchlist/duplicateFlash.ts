/** 「이미 이 그룹에 있습니다」가 가리킨 행을 하이라이트해 두는 시간(ms).
 *
 *  공용 `.row-flash`(global.css) 의 box-shadow 애니메이션이 2s 라, 그보다 길게 잡아
 *  애니메이션이 끝나기 전에 클래스가 걷히며 깜빡이지 않게 한다(스크리너의
 *  `NEW_ENTRY_FLASH_MS` 와 같은 근거).
 *
 *  **두 소비처가 같은 값을 써야 한다** — 관심종목 패널(드로어의 두 추가 팝오버)과
 *  편집 모달 우측 pane 이 같은 안내 문구("아래에 표시했습니다")를 쓰므로, 문구가
 *  약속하는 지속시간이 화면마다 다르면 안 된다. */
export const DUPLICATE_FLASH_MS = 2500;
