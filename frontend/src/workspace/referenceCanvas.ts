/** 페이지 중립 워크스페이스 코어 — 기준 캔버스.
 *
 * px 로 실측된 창 기본 크기를 **비율로 옮길 때 쓰는 기준 좌표계**다(ADR-0122). 실제
 * 캔버스가 아니라 기준이라는 점이 요점 — 1600×900 뷰포트에서 실측한 값이고, 두 페이지의
 * `DEFAULT_SIZE` 주석에 적힌 px 근거들이 전부 이 캔버스를 전제로 잡혀 있다.
 *
 * `/live` 와 `/study` 가 같은 값을 각자 들고 있었다. **같은 뷰포트에서 잰 같은 실측치**라
 * 갈릴 이유가 없고, 갈리면 두 페이지의 창 기본 크기가 조용히 어긋난다 — px 근거는 그대로인데
 * 비율만 달라지므로 화면에서만 드러난다.
 */
export const REFERENCE_CANVAS = { w: 1546, h: 776 } as const;

/**
 * 좁은 쪽 실측 캔버스 폭 — **절대 하한이 걸린 창의 비율을 역산하는 기준**.
 *
 * 비율 좌표계는 절대 하한을 표현하지 못하므로(ADR-0122), `BookPanel` 처럼 px 계약이
 * 있는 창의 기본 비율은 `REFERENCE_CANVAS`(1546, 넓은 쪽)가 아니라 **가장 좁은 실사용
 * 캔버스**에서 역산해야 한다. REF 로 잡으면 좁은 화면에서 계약을 못 채워 우측 요약 열이
 * 조용히 잘린다.
 *
 * 1280 뷰포트 실측값이다. 2026-08-17 여백 통일로 두 페이지 캔버스가 같아지면서
 * (둘 다 1208×704) 이 기준도 하나가 됐다 — 종전엔 `/live` 1226 · `/study` 1190 으로
 * 갈려 있었고, 둘 다 그 시절 레이아웃(탭 스트립·시세 스트립 포함) 실측이라 이미
 * 실제보다 컸다. **여백·크롬을 건드리면 이 값을 다시 재고 검산도 다시 할 것**
 * (`workspace.test.ts` · `studyWorkspace.test.ts` 의 하한 가드가 이 상수를 쓴다).
 */
export const NARROW_CANVAS_W = 1208;
