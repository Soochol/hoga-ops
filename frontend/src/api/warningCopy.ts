/**
 * kind → **원인 명사구**. 표면을 가로질러 같아야 하는 문구의 단일 출처 (ADR-0143 §5).
 *
 * ## 왜 이 파일이 생겼나
 *
 * 같은 `transport` kind 를 `/live` 토스트는 "시세 서버 연결 **불가**", `/screener`
 * 강등 배너는 "시세 서버 연결 **실패**" 라고 불렀다. 백엔드 사유는 문자열 하나인데
 * 표면마다 이름이 미묘하게 달랐던 것이다 — Phase 1·2 가 **진단**을 한 곳으로 모았으니
 * **어휘**도 같은 대접을 받아야 한다.
 *
 * ## 무엇이 여기 있고 무엇이 표면에 남는가
 *
 * 여기 있는 것은 **원인 명사구 하나**다. 그 원인을 어떻게 조립하는지는 표면이 정한다 —
 * 토스트는 제목+본문+상태 3단, 스크리너는 한 줄, 빈 상태는 한 줄+행동 1개(DESIGN.md
 * 빈 상태 규약). 같은 사실을 다른 길이로 말하는 것은 정상이고, **다른 이름으로 부르는
 * 것이 문제**였다.
 *
 * 그래서 처방 문구(`hint`)·본문·상태 라벨은 여기 없다. 그건 표면 맥락에 딸린다 —
 * 예컨대 스크리너의 "잠시 후 다시 조회하세요" 는 조회 버튼이 있는 화면에서만 옳다.
 *
 * ## 원인을 말한다 — 상태가 아니라
 *
 * `rate_limit` 은 토스트가 "시세 서버 혼잡"(상태), 스크리너가 "호출 한도 초과"(원인)로
 * 갈려 있었다. **원인 쪽으로 통일했다**(사용자 확정 2026-08-10) — 이 사전이 갖는 것이
 * 애초에 "원인 명사구" 이고, "혼잡" 은 무엇이 몰렸는지 말하지 않는다. 토스트 본문이
 * 이미 상태를 서술하므로("요청이 몰려…") 제목까지 상태일 이유도 없다.
 */
import type { LiveWarningKind } from './dataWarnings';

export const WARNING_CAUSE = {
  // ADR-0137 이 채택한 어휘다 — "연결 불가" 는 **닿지 못했다**를 뜻하고, 그래서
  // 혼잡(닿았지만 지금은 못 준다)에는 쓰지 않는다. 그 구분이 사용자 처방을 가른다
  // (우회를 켠다 vs 기다린다).
  transport: '시세 서버 연결 불가',
  // 벤더가 초당 한도로 거절했거나(`rate_limit_upstream`) 그 거절이 만든 우리 쪽
  // 쿨다운(`rate_limit_aborted`)이다. 처방이 같아서(기다린다) 백엔드가 한 kind 로 묶는다.
  rate_limit: '호출 한도 초과',
} as const satisfies Partial<Record<LiveWarningKind, string>>;
