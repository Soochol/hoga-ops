# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Playwright artifacts
playwright-report/
test-results/

# 워크트리 도그푸딩 전용 로컬 설정 — :5175 로 서빙하고 /api·/health 를 :8000 으로
# 프록시해 백엔드 CORS(:5173 전용)를 우회하는 일회용 파일이다. 세션마다 만들고
# 지우는 물건이라 추적 대상이 아니다(한 번 790d8718 에 섞여 들어간 적 있음).
vite.dogfood.config.ts

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vite-cache
